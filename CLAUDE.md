# bjorninn-quote-generator

Static HTML tools + Vercel API functions for Björninn Innréttingar. Deployed at
https://bjorninn-quote-generator.vercel.app — all pages talk to Airtable through
`/api/airtable`, a server-side proxy that holds the PAT (kept out of the repo
since GitHub push protection blocks committing an Airtable PAT directly).

## Pages (short URL → file)

| URL | File | Purpose |
|---|---|---|
| `/tv` | [dashboard.html](dashboard.html) | Framleiðsluyfirlit — production overview / TV dashboard |
| `/cutlist` | [cutlist.html](cutlist.html) | Skurðarlisti (Cutty) — cutting list, also drives USB export to the saw |
| `/aedar` | [grain.html](grain.html) | Æðaskipulag (office) — build & **confirm** a grain-matched front sheet layout |
| `/saga` | [grain.html](grain.html) | Same file in `FLOOR_MODE` — the floor opens a **confirmed** plan and runs the guided sawing steps (can nudge locally, no write-back) |
| `/labels` | [labels.html](labels.html) | Merkingar (Sögun) — label printing for cut pieces |
| `/skipulag` | [skipulag.html](skipulag.html) | **Saga — Skipulag** — office-side hub: pick Cutty or Æðar, project (`?recordId=`) forwarded into whichever you pick |
| `/verkstaedi` | [verkstaedi.html](verkstaedi.html) | **Saga — Verkstæði** — floor-side hub: pick Merkingar or Saga, project (`?recordId=`) forwarded into whichever you pick |
| `/leidbeiningar` | [leidbeiningar.html](leidbeiningar.html) | Leiðbeiningar — instructions for the cutlist + labels workflow |
| `/klukka` | [stimpilklukka.html](stimpilklukka.html) | Stimpilklukka — PIN-pad time clock (Inn/Út) for starfsmenn, kiosk-style; on ÚT, split the shift's hours across projects (→ `Verktímar 🕒`) |
| `/eigandi` | [stimpilklukka.html](stimpilklukka.html) | Same page in "owner mode" — phone clock-in for eigendur, no device pairing (PIN verified by [api/eigandi-clock.js](api/eigandi-clock.js), which also checks `Tegund starfsmanns = Eigandi 👨🏻‍💼`) |
| `/app` | [app.html](app.html) | **Verkstæðisappið** — installable PWA home screen for the floor. See "Verkstæðisappið" below. |
| `/verk-i-gangi` | [verk-i-gangi.html](verk-i-gangi.html) | Project picker for `/app` — lists projects currently in production, tapping one becomes the app's "current project" |
| `/skipuleggja` | [kitchen-planner.html](kitchen-planner.html) | **Eldhússkipulag (sjálfsafgreiðsla)** — customer-facing kitchen planner, see "Kitchen planner" below. Desktop only (<768px shows a gate). |
| `/eldhus-yfirferd` | [kitchen-planner-review.html](kitchen-planner-review.html) | Rakel's read-only review of a submitted plan (`?recordId=` of the Tækifæri); renders the same 3D/2D via the shared module |

Rewrites live in [vercel.json](vercel.json). To add another short URL, add a
`rewrites` entry there (`source` = short path, `destination` = the `.html` file),
then commit + push — Vercel redeploys automatically on push to `main`.

**Hub pages** (2026-09-12): `/skipulag` and `/verkstaedi` exist so an Airtable
button links to *one* URL per phase instead of one per tool. Each is a small
standalone page (same dark/Kumbh-Sans visual language as `velja.html`) with two
big choice cards; both read `?recordId=` and forward it unchanged into whichever
tool is picked — `cutlist.html`, `grain.html` (`/aedar`/`/saga`), and
`labels.html` all already auto-load a project from `?recordId=recXXXX` on their
own, so the hub doesn't need any Airtable write access, just the read-only
project-name lookup for the confirmation card. Adding a third tool to a phase
later = one more `.choice` block + one more forwarded href, not a rewrite of
the hub's logic.

## Kitchen planner (`/skipuleggja`)

HomeByMe-style self-serve planner that ends in a lead: steps intake → materials → room shape (wall chain) → **editor** → contact/submit. Submit writes Tengiliðir → Tækifæri (with the whole state as JSON in `Sjálfsafgreiðsla skipulag (JSON) 📐`) → one Eyðublað per cabinet, all through `/api/airtable` (server forces the review flag). Full history/gotchas live in the Claude memory file `project_kitchen_planner.md`.

- **Shared render module** [kitchen-planner-3d.js](kitchen-planner-3d.js) (`window.KP3D`): `CATALOG` (3 fixed-size active types + `legacy:true` oven/corner types kept so old drafts/submissions still render), `buildScene` (Three.js 3D: per-cabinet `THREE.Group`, fronts with procedural texture, door seams + handles, walls fade when between camera and room, floating-toolbar projection, cabinet/opening drag + click-select via `setupCabinetInteraction`), `buildPlan2D` (SVG plan), drag-preview helpers. Rakel's review page loads the same file with no callbacks (read-only).
- **Editor** (`screen-editor` in kitchen-planner.html): left catalog (click adds, drag drops into 3D or 2D), centre 3D/2D stage with floating toolbar and undo/redo, right panel = selected cabinet / window / door properties, or a room overview with quick material swatches. All mutations end in `refreshAfterEdit()`; state persists in `localStorage` (`bjorninn:kitchenPlanner:draft:v1`).
- **Free placement + islands** (2026-09-20): cabinets stand anywhere along a wall — `b.gapMm` = free space before a block (absent = flush; nothing auto-packs, `placeInWall`/`insertBlockAt`/`detachBlock` in kitchen-planner.html, `KP3D.blockStartsMm` is the one place offsets are computed). Arrows nudge 100 mm (Shift 10), swapping only when blocked. **Islands** = `state.islands[]` (`{id,label,lengthMm,xMm,zMm,rot,two,a[],b[]}`); `KP3D.surfacesOf(state)` returns real walls + island rows (row A, and B back-to-back) so all wall-based code (placement/drag/render/fit check) works on them; `surfaceGeoms`/`islandGeoms` are the matching geometry. Island rows are packed (no gaps) and the island's length is derived from its cabinets (`normalizeIslands()`, run in `save()`; grows from the right end); row B is measured from the far end so it gets a lead-in gap to line up with A. Slide an island with its yellow puck (3D + 2D) or the panel; catalog "Eyjar" (click = room centre, drag = drop point). Fit check warns on overlap and walkways < 900 mm. Real-mouse tests (Browser pane `computer` drags) found bugs synthetic events never did — use both.
- **Materials step = the site's own selection** (2026-09-20): [kitchen-planner-catalog.js](kitchen-planner-catalog.js) (`window.KPCAT`, generated once from the galleries on bjorninninnrettingar.is → Efnisúrval + Höldur; images in `materials/site/`, `handles/site/`) holds 49 fronts (Viðarlíki 18 / Framhliðaefni 23 / Spónlagt 8), 24 worktops (Límtré 6 / Compact 11 / Harðplast 6 / Steinn) and 14 handles. The site shows images only, so **labels are descriptive guesses from the photos — edit them in that file** (keys like `vl07`, `fh15`, `cp06` are what drafts store). `KP3D.LOOKS` merges them (old 4+1 Efnislisti looks stay as `hidden:true` for old drafts); `state.top` = worktop key (null = plain stone). Viðarlíki/límtré/compact/harðplast use the real photo texture in 3D (`imgTex`), Framhliðaefni is a satin colour, Spónlagt the procedural wood in the sampled colour. Handles are data-driven (`style` bar/edge/tab/knob/groove/none + `len` + `color`). New looks have no `efnislistiId`, so only the note to Rakel carries the choice.
- **Define your space** (2026-09-20, step 3, IKEA-style): shape cards (closed: rectangle / L-room / alcove; open: one line / corner / L, dashed = `wall.open`) → top-down drawing where any wall slides (arrows or drag) or its dimension is typed (popup with ◀ Vinstri / mm / Hægri ▶ = which end moves) → "Hvar er vatnið?" (water wall; `seedSink()` puts a Vaskaskápur there when the editor first opens). The room is a **closed rectilinear outline stored as the wall chain**: `state.closed`, per wall `lengthMm`, `turnAfter` (left = inward corner, right = reflex corner) and optional `open`. Sliding wall j by d only changes its neighbours: `len[j-1] -= d·turn[j-1]`, `len[j+1] -= d·turn[j]` (`slideLens`); `lensValid` rejects too-short walls, cabinets that no longer fit and self-intersection. 3D floor follows the outline (`ShapeGeometry`), open edges are dashed lines with no wall/skirting/cabinets/openings (`nearestWallDrop` skips them). Legacy open-chain states (old submissions on the review page) still render via `state.closed` falsy; old local drafts at shape/editor/contact are discarded on load. Only 90° walls exist — IKEA's chamfered/diagonal corner is not supported.
- **Materials = a wizard around one 3D cabinet** (2026-09-20): five pages (Skrokkur → Skúffukerfi → Framhlið → Höldur → Borðplata), each adds a level to a single spinnable 600×800×600 base cabinet (`KP3D.buildCabinetPreview/setCabinetPreview/teardownCabinetPreview`, reuses `addCabinetBox`): open box in the carcass colour → drawer bodies inside (Legra = metal grey, Merivo = white) → the cabinet closes with the front → handles → worktop. Preview level follows the page (`matPhase`), so going back shows the earlier, more open state. Drawer-system texts are the short placeholders in `DRAWER_SYSTEMS[...].desc` (to be written properly).
- **Real drawer/handle models** (2026-09-20): `kitchen-planner-models.js` (`window.KPMODELS`, empty) maps drawer keys (`legra_M|K|F`, `merivo_M|K|E`) and handle keys to `.glb` files in `models/drawers/` / `models/handles/`; `getModel()` lazy-loads them with GLTFLoader (via the importmap) and `normaliseModel` fixes units (mm → m), centres them and sits them on y=0 / front at +z (optional `rot:[x,y,z]`, `mirrorX`). No entry = the procedural drawers/handles. Load path verified with a generated test .glb (placement checked numerically). In the wizard preview each drawer is its own object (front 20 mm + handle + box) that slides out on click; the carcass is depth − 20 mm. NB: room scenes still draw fronts as one face on a 600 mm box.
- **Floors** (2026-09-20): `state.floor` → `KP3D.FLOORS` (3 parquets: light oak / natural oak / dark wide-plank walnut; 3 tiles: light concrete / beige stone / anthracite), picked in the size phase of step 3; procedural textures (`KPMat.plankFloorTexture(hex, rows)`, `KPMat.tileFloorTexture(hex)` = 2×2 tiles of 60 cm with grout). While a wall slides in the room drawing the view is held on a vertex that is not part of the moved wall (`frozenView.ref`), and islands are shifted by that re-anchor (`shiftIslandsBy`).
- **Editor round 2026-09-20 (late)**: the editor now fills the whole screen (sidebars flush to the edges, `#mainWrap.wide` = 100vh flex column). The progress bar is a numbered **stepper** (`#progressSteps .st`, `canGoTo`/`jumpTo`/`updateStepper`): click any earlier step, or a later one once its prerequisites exist (materials complete → shape; closed room → editor; ≥1 cabinet → contact); the kicker shows "Skref N af 5". Left sidebar = accordion (`renderCatalogPanel`, `openSecs` in localStorage): Breyta rými (floor swatches, wall colour, ceiling height, → shape step), Skápar, Hillur og úthliðar, Hurðir og gluggar, Eyjar, Efni og útlit. **All sizes are typed** in the properties panel (`setBlockDim`, ranges per type in `CATALOG` minW/maxW/minH/maxH/minD/maxD — oven, fridge, Töfrahorn included); the old `WIDTH_VARIANTS` stay only as `legacy:true` so old drafts load. Widening a cabinet grows it right, and LEFT when the right is blocked (`growBlock`). **Úthlið** (`uthlid`/`uthlidhar`/`uthlidefri`, `panel:true`, 19 mm, front material on every face) copies height/depth/elevation from the cabinet it butts against (`matchPanelToNeighbour`). **Lausar hillur** (`laushilla`, `shelfStack:true`): `b.count` 1–5 boards of 38 mm, `b.vgapMm`, `b.heightMm` = whole stack (`initShelfStack`); each board is its own pickable mesh sharing the block id. Wall units/windows have a height above the floor (`b.elevMm`, default 1400 / `CATALOG.elev`, windows `sillHeightMm`) and are **dragged up and down in 3D**: `wallPlaneHit` in kitchen-planner-3d.js casts the pointer ray onto the wall's own plane (front-facing hits only), which also fixed the "hard to move upper cabinets depending on the camera" parallax; drops carry `elevMm` (`snapElev` snaps to 1400 / neighbours' edges). **Wall selection**: click a wall in 3D (`opts.onWallSelect`, wall hit must be nearer than the floor hit) or 2D wall line → blue tint + frame (`setSelectedWall3D`), right panel = `renderWallPanel`; wall labels now read "Veggur N · mm". Fixes: open shelves got a front after select/deselect (`setSelected3D` swapped material slot 4 for the real front — now skipped for `u.open`); cabinet backs z-fought with the wall face (wall face now 1.5 mm behind the wall line); an island with no cabinets left is removed with its yellow puck (`normalizeIslands`).
- **Lock / gap / typed chip round (2026-09-21)**: (1) **Back fade** — cabinets seen from behind (camera on the wall side of their front plane) turn ~84 % see-through with only the outline left (`fadeCabinets`/`ghostApply` in `buildScene`'s loop, per-cabinet material clones swapped in and out; the selected cabinet and anything being dragged stay solid). (2) **Size chip is editable** — click the "600 × 800 × 600" chip on the floating toolbar → inline W×H×D inputs (W×H for windows/doors/openings), Enter/blur applies through `applyBlockDim` / `applyOpeningDim`, Esc cancels. (3) **Op í vegg** (`state.doors[]` entry with `gap:true`, catalog "Hurðir og gluggar") — a real hole: `addWallPlane(..., gaps)` builds the wall in pieces (left / header above / right) and `addOpeningMarker` draws only reveals + an invisible pick plane; 2D shows two end ticks; counted separately from doors in notes/summary. (4) **Ofnaskápur** default height 2400. (5) **Töfrahorn** has ONE door on the left or right half (the other half is a fixed blind panel): `b.swing` = `vinstri`/`haegri` (the same field as "Lamir hurðar" on other units), toggled in the side panel ("Hurðin er á"). (6) **Lock** (`b.locked`, 🔓/🔒 in the toolbar and a full-width button in the panel; padlock badge over the cabinet): a locked unit can't be dragged (3D or 2D), nudged, swapped, resized, retyped, moved to another wall or deleted (`lockedToast` guards in kitchen-planner.html; panel inputs are disabled). (7) **Locked cabinets open and close** — `addCabinetBox` builds them as parts (`art`): open carcass (depth − 20) + 20 mm front slabs with handles + drawer boxes (`buildDrawerBox`, real Blum heights for 3-drawer units) + shelves; `addArticulated` in kitchen-planner-3d.js. Tap a drawer front/handle/box = slide 0.34 m, tap a door = swing 100° on its hinge (`b.swing`, wide units ≥ 750 mm = two leaves); a tap on the carcass selects. Open/closed state lives in `PART_STATE` (keyed "blockId:part") so it survives scene rebuilds. Oven, open shelves and end panels only lock (no parts). Unlocked cabinets are still the single solid box.
- **Flow + welcome + collisions round (2026-09-21, later)**: (1) **Order is now room → materials → layout**: `FLOW = intake → shape → materials → editor → contact` (5 stepper pills, `STEP_ORDER` adds `welcome` in front and `done` at the end); `canGoTo`: materials/editor/contact need a closed room, editor also needs materials complete. New **welcome screen** (`screen-welcome`, `#welStart`) with the three stages; fresh state starts there. (2) **Room step phases**: pick → size (mm, ceiling height, floor, wall colour) → **openings** (new: windows / doors / "Op í vegg" with wall picker, offset/width/height/sill inputs per opening, drawn on the top-down canvas, wall number badges `data-openwall`) → water → button "🔒 Læsa rýminu — velja efni" (`lockRoom()`; there is no hard lock flag, the editor's "Breyta rými" still reopens it). Openings can still be edited in the editor. (3) **Progress bar** = `.stepper .bar` filled by `updateStepper` from `(stepIndex + stepFrac())/5` (`stepFrac` = progress inside the current step: quiz answers, shape phase, material page, cabinets placed); finished pills are tinted. `.stepper[hidden]` needs its own `display:none` (author `display:flex` beats the hidden attribute). (4) **Upper units / open shelves and tall floor units block each other**: `probeSpan`/`crossSpans`/`crossOverlap`/`crossBounds` in kitchen-planner.html feed `freeIntervals`/`placeInWall`/`canFit`/`appendPlacement` (pass a `probe` {lo,hi} for new blocks, existing blocks derive it from `excludeId`), nudge/grow, height/elev/type changes are refused when they would collide; `fitWarnings` also flags such overlaps in old drafts ("X rekst á Y á vegg N"). (5) **Plinth/skirting glitch seen from behind**: the skirting board's and the plinth's back faces both sat exactly on the wall line (z-fight, checker pattern); skirting is now 2 mm behind the wall line and the plinth starts 3 mm off it. (6) Overview title is "Eldhúsið mitt".
- **Real Blum drawer sides (2026-09-21)**: `KPMODELS.drawerSides["merivo_E"]` = Blum `.dae` downloads (one file per side, white/dark, in `models/drawers/`; SolidWorks export in mm, one side = 15 mm wall + 192 mm tall incl. the foot clip, 493 mm long). `buildDrawerBox(..., code)` places the real left/right sides at the box edges (outer edge at ±bw/2, bottom y=0, front z=0) and adds a plain bottom + back between them; used only for that height code and only when the cabinet is deep enough (box ≥ 490 mm), otherwise the procedural drawer. `rawModel()` loads without re-centring (parts keep their relative positions) and turns Phong into Standard materials; `.dae` and `.glb` both load (`loadRaw`). Since then: all Legra (M/K/F/C) and Merivo (M/K/E) sides are in (`drawerSides` in kitchen-planner-models.js); the Blum "L" file (x<0) goes on the RIGHT of the drawer and "R" on the LEFT (foot points inwards); `rawModel` measures each side's wall (top edge) so the plain bottom/back fit between the walls, the back is as tall as the sides, `boardMm` = bottom height. Slide runners (`KPMODELS.runners`, Legra 750.5001S / Merivo 450.5001B) are children of each side's holder (same frame as the sides, paired by the same L/R letter) and stay FIXED in the cabinet (only the drawer sides slide). **Merivo K white** = the dark K files with `recolor:"white"` (dark non-metal materials → Blum's silk white, in `rawModel`). The small cover caps are NOT used yet. Real parts are not scaled to other cabinet depths/widths; handles still to come.
- **Room step layout (2026-09-21)**: drawing on the left, ONE panel on the right (`.shape-wiz` grid: `#shapeSide`) like the other steps — size phase: wall list with typed lengths (`renderWallList`, moves the wall's end via `setEdgeLength`), ceiling height, floor, wall colour; openings phase: add buttons + per-opening rows (wall, offset, width, height, sill); water phase: radio list of walls. The action buttons (`#shapeActions`) move into the panel while drawing. **Openings are draggable on the 2D drawing** (`startOpDrag`, snaps to the nearest wall, keeps the grab point) and their measurements are clickable pills (width; for the selected one also gap to wall start/end → `openOpPop` popup, Enter applies); the panel rows stay in sync. **Drawer runners are fixed in the cabinet** (`g.userData.runners` added to the cabinet frame, not the sliding drawer).
- **Water is a draggable marker (2026-09-21)**: `state.waterWall` + `state.waterAlongMm` (centre of the sink along the wall, null = middle). In the room step's water phase click any wall / drag the droplet along the walls (`startWaterDrag`, snaps 10 mm, can jump to another wall), the 800 mm sink footprint is shown on the wall, distance pills to both wall ends are clickable (`openWaterPop`), and the panel has a typed position field. `seedSink()` centres the Vaskaskápur on the marker (clamped to the wall / corner clearance) when the editor first opens with an empty room.
- **Real handles (2026-09-21)**: `KPMODELS.handles[key]` = `{file, kind, map, color, stripMm}` (kitchen-planner-models.js); `handleProfile()` in kitchen-planner-3d.js orients a model into the handle frame (x along the front, y up with the TOP edge at 0 for Jey, z outward) via `map` (which model axis becomes x/y/z, `-` flips) and stretches it along x. Wired: **jey2** ← `jey.glb` (kind `jey`: full-width J-profile that REPLACES 27 mm of the front — in locked cabinets the slab is 27 mm shorter, or 27 mm narrower on tall units), **kantgrip_kopar_tab** ← `comet.glb` (kind `bar`), **hexxa** (kind `hexxa`, no model: a milled slot centred with 50 mm margin each side, length = front − 2×margin). **On tall units (`isTall`, fridge; not ovens) pulls stand vertical on the free side (opposite the hinge)** — also the drawn bar/edge/tab/groove styles (`vertical`, `edgeAlong`); two-leaf units get one per leaf next to the centre seam. `place()` overwrites a mesh's rotation, so turned handles keep the rotation in a child group. Files not mapped yet (in `models/handles/unmapped/`): viefe-vann-0375, viefe-angle-0077, viefe-graf-big-1200, brass-bar-sans-titre; the 10 MB Crossing Handle 32 cm is NOT in the repo (embedded wood texture — needs stripping first).
- **Handles restarted (2026-09-21, later)**: the handle range is rebuilt one handle at a time — each = thumbnail + .glb + description — in [kitchen-planner-handles.js](kitchen-planner-handles.js) (`KPHANDLES`: `groups` = the picker tabs **Ofan á höldur / Áfestar höldur / Grip / Án handfangs**, `items` = the visible handles, `placeExisting` = Falið grip + Þrýstiopnun live under "Án handfangs"). Every earlier handle (Ona, Jey2, Arpa, Hexxa, the 14 site ones) is `hidden:true` in `HANDLES` — old drafts/submissions still render — and the picker (`renderHandleCards`, `matTab.handle`) only lists visible ones per group. Model settings per handle stay in `KPMODELS.handles` (kitchen-planner-models.js). **Comet** = first new handle: group `ofan-a`, `kind:"topmount"`, 200 mm, thumbnail `handles/comet.jpg`, model `models/handles/comet.glb` (map y→x length, z→y thickness flat side up, x→z the 40 mm it projects); sits ON the top edge of every front (each drawer, each door; two-door units get one per leaf, centred; 200 mm unless the front is narrower), upside down on the bottom edge of wall units, and on tall units an upright edge pull on the free side at ≈1.05 m. To add the next handle: picture in handles/, model in models/handles/, an entry in `KPHANDLES.items` + `KPMODELS.handles`, plus a `kind` branch in `addFrontDetails` if it needs new placement rules.
- **Handles fed in one at a time (2026-09-21, evening)** — now in the picker (`KPHANDLES.items` + `KPMODELS.handles`): **Ofan á höldur**: Comet (200 mm), Vann (200 mm copper flip: `skip:2, take:2` picks the 200 mm pair out of the file, rotated like Comet, `embedMm:20` = its 90° corner sits inside the front); **Áfestar höldur** (`kind:"bar"`, centred, `lenMm` = the real product size, shortened to `front − 2×fitMarginMm` on narrow fronts, upright at ≈1.05 m on the free side of tall units): Angle (600 mm profile, black), Crossing (328 mm, black brackets stand off with the oak bar at the outer end — rotated 90° about its length after review; the 10 MB original was stripped of its texture → `crossing.glb` 45 KB), Graf Big (`posts:true` — the 1200 mm model is bar + 3 posts, `handleProfile` bakes it, keeps the two END posts and scales the bar to 320 mm), Sense Big (`dropFlat:true` removes the big flat plane that came with the file); **Grip**: Jey (`kind:"jey"`, takes its own height — 37 mm — off the front and sits on top of it, finger lip at the bottom of the profile, brass) and Hexxa (`kind:"hexxa"`, milled recess drawn procedurally, 50 mm margin each side — keeps its old key so the Vörulisti id still works). Thumbnails are 4:3 crops of Viefe's product photos (`handles/*.jpg`). Model orientation is set per handle with `map` (see comments in kitchen-planner-models.js); Angle/Crossing/Graf/Sense/Vann orientations were chosen from bounding boxes + the photos and only checked visually in the wizard preview.
- **Later (user, 2026-09-20)**: once more products/options exist, offer customers **suggestions** (e.g. "add an end panel here", "most kitchens like this have X"). Not started — revisit after the catalog has grown.
- **Adding a cabinet type**: add it to `CATALOG` (fixed W/H/D, `zone`, `cls`), list it in `CATALOG_GROUPS` in kitchen-planner.html, and map any Airtable fields in `collectEyðublaðRows()`.

## API

- [api/airtable.js](api/airtable.js) — proxy for Airtable requests (holds the PAT server-side)
- [api/generate-quote.js](api/generate-quote.js) — quote PDF generation (uses `pdf-lib`, `maxDuration: 30` in vercel.json)
- [api/version.js](api/version.js) — returns the deployed `VERCEL_GIT_COMMIT_SHA` (`no-store`). `/klukka` and `/tv` both poll it every 3 min and reload when it changes, so those always-open tabs never run a stale build. `/klukka` defers the reload until it's idle on the PIN screen (`reloadPending` → `resetPin()`); `/tv` has no user interaction so it reloads immediately. First rollout onto an already-open tab still needs one manual reload (the checker has to be running to catch the *next* deploy).

## Workflow notes

**Verkstæðisappið — installable PWA home screen (2026-09-16)**: user asked to turn the workshop's scattered tools (stimpilklukka, active-project list, fræsing, merkingar, the skipulag/verkstaedi hubs) into one "app" instead of several separately-bookmarked/QR-reached pages, with two explicit requirements: (1) picking a project once should carry it into every tool without re-picking from a dropdown, (2) adding a quick task + assignee had to be genuinely easy — described as "stór hluti" of the ask. `/app` (app.html) is the result — a real installable PWA (`manifest.json`, `sw.js` — deliberately caches nothing, just satisfies Chrome's install criteria — and icons generated from the company logo under `icons/`, `Add to Home Screen`-able on iOS via the meta tags). Its **current project** is picked on `/verk-i-gangi` (verk-i-gangi.html, same eligibility filter as grain.html's `fetchEligibleProjects`: won + not for-skipulag + not yet delivered) and persisted in `localStorage` (`verkApp:project`) for as long as the app stays open — every tool tile (`Merkingar`→`/labels`, `Yfirfræsing`→`/fraesing`, `Skipulag`→`/skipulag`, `Saga`→`/verkstaedi`, `Verk — yfirlit`→`/verk`) forwards it as `?recordId=` (or `?t=` for `/verk`) automatically, no proxy or target-page changes needed since every one of those already auto-loads from that param. `/app` itself also accepts `?recordId=` on landing (same as the other hubs) so an Airtable button can still deep-link straight in.

The QUICK FIX❗ Airtable table already existed (dashboard.html reads it read-only for the TV alert bar) but had no create path — `api/airtable.js` gained `CREATABLE_FIELDS`/`FORCED_CREATE_FIELDS` (Staða forced to "opið 😠", same self-approval-proofing pattern as Orlofsbeiðnir) and `WRITABLE_FIELDS` (Staða only) entries for it. `/app`'s add-task form is always visible (not collapsed behind a button — the "auðvelt" requirement) with the assignee dropdown (`Ábyrgðarmaður`, the fixed 6-person singleSelect) right under the task text, optional deadline/reporter, and auto-links `Tengt verkefni` to the current project when one's selected. A "Nýleg verkefni" list right below it shows every non-`Lokið` QUICK FIX, tap-to-advance `opið → Í vinnslu → Lokið` — closes the loop without needing Airtable open. Verified end-to-end against the live base via `vercel dev` (project picked → recordId forwarded into a real labels.html load with the dropdown skipped; a real QUICK FIX created, read back, and deleted; the status-tap PATCH verified against a disposable test record, never against real in-progress rows) rather than just eyeballing the UI, since this writes to the same base every other floor tool depends on.

- cutlist.html and labels.html are meant to be used together — see leidbeiningar.html for the intended order of operations.
- grain.html (`/aedar`) is a V1 standalone planner for veneered/woodgrain fronts, which Cutty can't nest (it rotates pieces freely and has no notion of sequential grain matching). It reads Sögunarlisti front rows + Efnislisti board sizes, groups fronts into **grain runs** by `Eyðublað` unit × rounded width `B`, expands by `M`, and auto-nests each run as one composite panel (grain axis = board length, no rotation) with a guillotine strip packer. Overlong runs auto-fall-back to side-by-side (`orient:h`) then to individual fronts. **Drag & drop on the sheet**: each run has a ⠿ handle (top-left) that drags the whole run — smooth (only the `<g>` is transformed mid-drag, layout commits on drop), snaps to trim + neighbour edges, drop on another board to move it, drop a run's edge onto a same-width run to merge. **Nothing can overlap**: `nearestFree(bi, gid, want, W, H)` — used on every run/front drop — returns the closest position where the box doesn't overlap any run (obstacle-edge candidates + a coarse grid scan, then `snapPos`); `resolveCollision` is an alias. Every structural op (rotate/split/merge/send/reconcile) runs `settleBoard`/`settleAll`. **Drag ghost**: a `#dragGhost` fixed div follows the cursor across every board during a run *or* front drag (`ghostShow/Move/Hide`), the source is dimmed to 0.22, `body.dragging` forces the grabbing cursor — so a front stays visible while you carry it to another sheet. **Snap grid**: `settings.grid` (default 25 mm, toolbar input) — runs lock to grid multiples while dragging; neighbour/trim edges stay magnetic first for tight butt joins. The "⊞ Grið" toolbar button toggles `settings.freeMove` → "✋ Frjáls færsla" = drop anywhere (0.1 mm), no grid lines, edges not magnetic. Faint grid lines drawn on the board SVG when not in free mode. Both persisted in `grain:settings`.

**On-screen board size**: `settings.boardPx` (default 440, persisted per device) drives `--board-w` on `.board-wrap`; a "Stærð á skjá" range slider in the toolbar sets it, and each `.board-wrap` also has `resize:horizontal` so you can drag its corner — a `ResizeObserver` syncs that back to the slider + settings. `#boardsCol` is `display:flex; flex-wrap:wrap` so boards sit **side by side** and wrap to the next row when they don't fit. Each run has a dark **header band inside its top edge** (`HB≈19` viewBox units) carrying the unit label + ⠿ handle, so it never collides with neighbours or clips off the board. Front-cell labels (font-size 18) are `unit · shortPartur · W×H` → `unit · W×H` → `W×H` → `#N` as space allows, drawn inside the cell below the header band (`topInset`), flat or rotated up the left edge for portrait cells. `settings.trim` defaults to 10 ("Jaðar-snyrting (sjálfg. 10)") and `loadSettings` coerces a stored 0/blank back to the default.

**Woodgrain**: each board SVG has a soft procedural woodgrain background (`feTurbulence` fractalNoise stretched vertically → brown `feColorMatrix`, ~0.32 opacity over `#171310`) so the sheet's grain direction (down the length) is always visible; per-front grain streaks sit on top. Each **front cell** is its own drag target: drop on empty board space → pops into its own single-front run at that spot; drop onto a same-width run → joins it at the nearest slot; drop back over its own run → reorders. Front rows in the side panel are HTML5-draggable to reorder within / move between runs. A run's `orient` is one of three (panel shows 3 buttons: `setOrient(gid, o)`): **`"v"` stack** (drawer bank — grain down the length, crosscut off the top in sequence); **`"s"` side by side, upright** (wing / split doors — panels left→right, grain vertical in each, figure continuing across the seams; cut as one tall piece then ripped into panels); **`"h"` rotated 90°** (grain across the board width). Geometry via `frontExtent`/`runSpan`/`runTall`; `groupWidth`/`groupLen` branch on all three. `Partur` matching `SIDEBYSIDE_RE` (`væng`) defaults to `"s"` (wing doors — horizontal pair); `KEEPTOGETHER_RE` (`væng`/`skipt`) also covers "skiptur frontur" (vertical split) which stays `"v"`. Both get `keepTogether:true` (`g.kind` = "væng"/"skiptur" for the panel label) — `normalizeGroups` re-arranges starting from the run's own orient but **never auto-splits** them. The cut plan + print sequence have a branch per orient. Grain streaks are drawn in each front cell (horizontal only for `"h"`); narrow portrait cells get the label rotated up the left edge.

**Two-step office → floor** via the `Æðaplan 📐` table (`tblzkw70E2xoX9RmK`: `Nafn` / `Tækifæri 📣` link / `Efni` = material key / `Skipulag` = layout JSON / `Staðfest ✅` checkbox — all 3 proxy allowlists + `REQUIRE_FILTER`; one row per project+material). `/aedar` (office): any edit auto-saves the layout JSON to the row as a draft (debounced 1.4s, un-ticks `Staðfest ✅`); **"✅ Staðfesta skipulag"** ticks it — but `confirmPlan()` **hard-blocks** on (a) any group not on a board (`unplacedGroups()` — those fronts would never reach the saw) and (b) any overlap (`detectOverlaps()`); only mild overflow past the trim line gets a soft "staðfesta samt?" (fine on melamine). `/saga` (floor, `FLOOR_MODE = location.pathname === "/saga"`): pick a project → loads its ticked plan (skips Sögunarlisti entirely, rebuilds groups from the JSON) → lands on the **sheet overview** (not straight into the steps — the floor plan-bar has a "▶ Byrja sögun" button); `body.floor` hides the `.office-only` toolbar buttons. The floor **can still nudge** (drag/orient) — changes save to that device's `localStorage` only, stamped with a `planSig` hash of the confirmed JSON so a re-confirm from the office wins over stale local tweaks; **"↺ Sækja staðfest skipulag aftur"** discards them. `serializePlan`/`applyPlan` carry `{board, settings, groups, layout}`. The `loadingPlan` flag stops `applyPlan`'s `reconcileLayout→saveLayout` from firing a spurious plan-save (which would un-confirm on open). Unit names: the proxy returns Sögunarlisti's `Eyðublað ✏️` link as bare ids, so `fetchUnitNames` fetches Eyðublað (`tbl0WcyHhz63pSzZX`, primary `Athugasemd`) to map id→"Sk 20-22".

**Hand-added fronts** (plan-only, never touch Sögunarlisti): "＋ front" on a group or "＋ Nýr stakur front" in the side panel adds a `manual:true` front (green dashed on the sheet, editable H/width in the panel, ✕ to remove). Tracked in `manualFronts` (`{id,groupId,unitName,w,orient,label,h}`), persisted to `grain:manual:<proj>:<key>` in localStorage and folded into `groups` by `mergeManualFronts` — survives "Núllstilla röðun". `syncManualIndex` (called from `saveLayout`) keeps the index current after drags; `serializePlan` carries `manual`/`mfId` per front so an office-added front reaches `/saga`. They appear in the cut plan / print / guide like any front. Not on the Cutty cutlist, labels, or Pöntunarlisti — for that they'd need a real Sögunarlisti row (future: duplicate an existing row rather than create, since `Efni:` is a big derived formula).

**Project filter** (`fetchEligibleProjects`): `Staða í söluferli = "🏆 Tækifæri unnið"` AND `Staða í skipulagi != "For-skipulag"` AND `Staða í framleiðslu` not in `{"Afhent (LOKIÐ ✅)", "Má Afhenda 🚚"}`.

**Front detection is material-aware** (was a Partur keyword guess): a row is a front if `FRONT_RE` matches the Partur (`frontur|blindlok|áfella|vænghurð|sökkul cover`) **OR** `p.frontMat` — which is true when the Partur has `FRE`/`FRE2` (`FRE_RE`, = cut from frontaefni) **or** the row's resolved material's `Undirflokkur 🗂️` is in `FRONT_SUBS` = `{Frontaefni, Spónlagt, Hurð}`. `materialSub(efni)` looks the Undirflokkur up from `boards` (exact norm name → startsWith → token overlap), cached in `_subCache` (cleared per project load). `fetchBoards` now also pulls `Undirflokkur 🗂️` (added to Efnislisti's proxy allowlist); the proxy returns single-selects as a bare string so the boards map does `r.fields[E_SUB]?.name || r.fields[E_SUB] || ""`. This means FRE carcase parts ("FRE - Bak/Hlið/Toppur/Botn/Föst Hilla") are now included as fronts — they're on the same æðaefni sheet and want grain continuity. **Skipped fronts**: a row that *is* front-material (`p.frontMat`) or literally `\bfrontur\b` but got dropped (missing H/B) lands in `skippedFronts` → `renderSkipped()` warning card. `MAYBE_FRONT_RE` was removed — it flagged every "Skápur hlið" (29 false positives on T-15). **New-fronts check** (`checkNewFronts`): after a saved plan loads, its pieceIds are compared against a fresh `buildGroups()`; any Sögunarlisti fronts the plan is missing show a blue "🆕 N nýir frontar" card inside `#skippedCard` with a "Bæta þeim við skipulagið" button (`addNewFronts` folds them into groups + drops on the last board + `settleAll`). **Grain detection** (`isGrainMaterial`): `Undirflokkur = Spónlagt` → grain; then décor code `H####` = woodgrain, `U####` = solid; `GRAIN_RE` species keywords are the fallback. It's only a picker sort/badge — you can still plan a "slétt" material.

**Recent UX**: `nearestFree` on every drop (no overlaps ever); undo/redo (⌘Z / `pushHistory` from `saveLayout`); confirm dialogs on "Núllstilla röðun" and "Fjarlægja plötu"; manual fronts have an editable label (`mf-label`) + `duplicateFront(gid,fi)` (⎘, works on real fronts too → makes a manual copy); `copyFloorLink()` on a confirmed plan; arrow-keys nudge the selected run by the grid step (Shift = 1 mm, Delete removes a lone manual run); guide top bar has per-board jump buttons + "N búin" count.

**Safety batch (2026-09-09)**: `unplacedGroups()`/`unplacedFrontCount()` — groups on no board show an orange `#overflowWarn` box ("N frontar eru ekki á neinni plötu … verða ekki sagaðir"), block `confirmPlan()`, and prompt in `openGuide()` before starting. Overlaps hard-block confirm (were a soft "samt?"). Material picker shows only grain materials; flat ones sit behind a "Sýna slétt efni (N)" toggle (`#showFlatBtn` → unhides `#flatMats`). Toolbar "← Efni" (`backToMaterials()`) returns to the picker without dropping the project (for a project with 2 grain materials). Yield stat is coloured (green ≥68 / amber ≥50 / red). Board grain indicator is now a drawn arrow + "æð" label (was the cryptic "◄ VIÐARÆÐ ►" text) — in `renderBoards` and `guideSvg`. Guide `produces` chips show `n/of` for multi-front runs and the label reads "merktu strax á bakhlið: eining, ör upp ↑, og númer í röð".

**Cross-system batch, HTML-only (2026-09-09)** — no new Airtable fields (base is due a trim); the one schema-adjacent change is adding `Sögunarlisti."Skilaboð til skipulags"` to the proxy allow/writable lists in `api/airtable.js`.
- **#5 floor re-poll** (`checkFloorPlan`, 90 s, `/saga` only): re-fetches the confirmed `Æðaplan` row; a red `#floorUpdBanner` appears if the office edited (`planSig` vs `floorPlanBaseSig`, set in `openFloorPlan`) or un-ticked `Staðfest ✅` while the sawyer has it open. "Sækja nýtt skipulag" → `reloadFloorPlanFresh()` (drops local tweaks, re-applies).
- **#11 reactive (no proactive Airtable flag)**: (a) `populateProjects` → `annotateProjectsWithPlans` fires one `OR(FIND(...))` query and tags each `<option>` with "✏️ drög" / "✅ skipulag". (b) `buildMaterials` writes `#planNeedNote` — "⚠ N af M æðaefnum á eftir að skipuleggja og staðfesta: …" (or a green all-clear).
- **#6 flag-a-problem-front** (`flagPiece(pieceId,label,unitName)` / `flagFront(gid,fi)`): a ⚠ button on each side-panel front row and each guide `produces` chip → prompt → read-append-PATCH the front's existing `Sögunarlisti."Skilaboð til skipulags"` (stamped `[gólf|skipulag dd/mm/yyyy]`, rolls up to the project for the planners). Office `/aedar` reads them back into `#planMsgCard` ("💬 N skilaboð um fronta"); `_flaggedPieces` shows a 💬 on flagged rows this session. Manual fronts (no `pieceId`) can't be flagged.
- **#7 guide datum labels**: `buildCutPlan` post-pass adds `step.datum` ("frá síðasta þverskurði" / "rif-fence = þessi tala" / "af ytri brún") and, for crosscuts with an h cut-line, `step.absStop` = kerf-inclusive cumulative mm from the trimmed front edge. `renderGuide` shows both in a `.gdatum` box. **The arithmetic is internally consistent but wants a sawyer check on one real sheet before it's trusted as a fixed-stop number.**

**Office simplicity pass (2026-09-12)** — "make the office flow extremely simple and quick": **materials.html is now melamine-only** — `isMelamineGrain(name)` (was `isGrainMaterial`) returns `false` for `Undirflokkur=Spónlagt` (real veneer needs book-matching this tool doesn't do) as well as flat/solid U-code décor; `buildMaterials()` filters `materials` down to that set instead of sorting grain-first with a "sýna slétt efni" toggle — non-melamine materials are never listed at all. **No more "veldu efni til að skipuleggja" picker step** — `buildMaterials()` now calls `selectMaterial(materials[0].key)` itself the instant the (already-filtered) list is known, so a single-material project (the common case) goes straight from picking the project to the planner. `renderHeaderPill()` renders a combined project+material pill in `#projectCard` (`#matSwitcher`): the matched Efnislisti board's photo (`E_MYND = "Mynd 🖼️"`, same `att.thumbnails.large.url` extraction as `eining.html`, falls back to a 🪵 swatch div if no photo) + material name + plan-status badge + a `<select id="matSelect">` that only renders (and only then) when a project has more than one melamine material — `onchange="selectMaterial(this.value)"`. `#materialCard`/`#materialList`/`#materialCardLabel` still exist but are **floor-only** now (`loadFloorPlans`'s "pick a confirmed plan" chooser when a project has >1) — office never shows them; `backToMaterials()`/"← Efni" were removed since there's no picker to go back to. `#planNeedNote` (the "N af M óskipulögð" banner) was removed entirely in the next round below — the ✏️/✅ badge in the header pill already says that.

**Header-pill round 2 + more decluttering (2026-09-12)**: `#projectCard`'s `.hdr-row` puts `#projectName` and `#matSwitcher` **on one line**, `justify-content:space-between` — name on the left, material chip in the right corner. Both sides ellipsis (`text-overflow:ellipsis` + a `title` attribute carrying the full text) instead of wrapping, so neither a long project name nor a long décor name can force the material chip onto its own line — first attempt used `flex:1 1 auto` on the name with no cap, which let it claim the *entire* row and push the chip below every time; fixed with `max-width:60%` on the name and `max-width:200px` on the material name/badge. `#planNeedNote` (the "⚠ N af M æðaefnum óskipulögð" banner) is gone — the header pill's ✏️/✅ badge already says that, a second banner was noise. `renderPlanMsgs()` (`#planMsgCard`, "💬 N skilaboð um fronta") is now a native `<details>/<summary>` — collapsed by default instead of an always-expanded list, since it's a to-do/FYI, not an urgent warning. `#confirmBtn`/`#guideBtn` sit next to each other in the toolbar's right corner now — only `#confirmBtn` keeps `margin-left:auto` (pushes the *pair* right), `#guideBtn` lost its own `margin-left:auto` (was fighting `confirmBtn` for the same free space, which is why they weren't reliably adjacent before). **Popover close was reported "stuck" again after the first fix** — hardened further: `.pop-close` dropped the negative-margin sizing trick for a plain 34×34 box, and the *entire* `.pop-head` row (not just the ✕) now calls `closeAllPopovers()`, so there's always a big, unambiguous target. The open→close mechanism itself was re-verified via a full synthetic pointerdown→mousedown→pointerup→mouseup→click sequence (not just `.click()`, which skips pointer events entirely and would have missed a real interaction bug) and works correctly — if it still doesn't close on a real device, suspect a stale cached copy over an actual logic bug and try a hard reload first.

**Popover round 3 — found the actual bug (2026-09-12)**: the user sent a screenshot with *both* popovers open on page load, before any click — the previous "verified working" testing only ever checked the `hidden` **attribute/property** (`p.hidden === true`), never the **computed style**, and that's exactly where the real bug was hiding. `.settings-pop { display: flex; ... }` is an *author-stylesheet* rule with no condition on it — and author CSS always wins over the browser's built-in `[hidden]{display:none}` rule regardless of specificity (origin beats specificity in the cascade). So the JS was toggling the `hidden` attribute correctly the whole time, but it never had any visual effect, on any device, ever — not a caching issue, not a device quirk, a straightforward CSS cascade bug. Fixed with `.settings-pop[hidden] { display: none; }` (class+attribute = higher specificity than `.settings-pop` alone, so it wins outright). Re-verified this time via `getComputedStyle(el).display` at every step of a real open→close cycle (was `"flex"` when open, `"none"` when closed, for both popovers, both close paths) — not just the attribute. Checked the rest of the file for the same pattern: everywhere else uses the `.hidden` *class* (`display:none !important`), which is immune to this since `!important` always wins regardless of origin — `#settingsPop`/`#morePop` were the only two elements using the bare `hidden` HTML attribute, so this was the only place the bug could hide. **Lesson for future popover/hidden-state work in this file: always assert on `getComputedStyle().display`, never just the `hidden` property** — the property can be "correct" while the element stays fully visible. User also asked whether `<details>`/`<summary>` (used for `#planMsgCard`) would be more robust here — for a simple static list, yes; for a positioned floating overlay like these two, it still needs the same "don't declare an unconditional `display` that fights the browser's own hide mechanism" discipline, so it wouldn't have been automatically bug-free either. Kept the popover pattern since the actual bug is now fixed and understood, rather than swapping patterns and re-verifying a new one right before real use.

**Drag-feel polish (2026-09-12)**, in response to "can you make the dragging movement any nicer?":
- **`#dragGhost` now transitions in/out** instead of a hard `display:none`↔`block` toggle — always in the DOM (like `#toast`), `.show` class drives `opacity`/`transform: scale(1.05)`/`box-shadow` with a `.12s` transition, giving a small "lift off the board" cue on pickup and a fade on release. `ghostShow()` only sets size/colour/text now; the *first* `ghostMove()` call after it positions the ghost synchronously (no stale-position flash) and *then* schedules the reveal one frame later so the transition has a real "before" state to animate from — get this ordering wrong (reveal before position, or position via the batched path below) and it flashes at the wrong spot for a frame.
- **Cursor tracking is now batched through one `requestAnimationFrame`** (`ghostRAF`/`ghostLast` in `ghostMove`) instead of updating `left`/`top` synchronously on every raw `pointermove` — `pointermove` can fire faster than the screen repaints, so this coalesces a burst into one paint per frame, which matters most on a slower shop tablet.
- **`pulseLanded(gid)`** — a `.4s` `landPulse` brightness-flash `@keyframes` on `.grgbox`, self-removing via `animationend`. Called after every successful drop (`enableDrag`'s and `enableFrontDrag`'s `up()`, after `saveLayout(); render();`) so a drop reads as "it landed here" instead of silently snapping into place — `render()` tears down and rebuilds every board SVG from scratch each time, so there's no DOM continuity to animate a true position transition *from*; a flash is the honest substitute. `maybeMergeOnDrop()` now returns the id the dragged run actually ended up in (was a bare `true`/`false`) so the pulse still lands on the right element when a drop triggers a merge.

**Readability + decluttering (2026-09-12)**: all the `#999`/`#888`/`#777`/`#aaa`/`#bbb`/`#ccc` low-contrast grays used for actual text (labels, hints, meta, guide chips) were bumped to `#d5d5d5`/`#cfcfcf`/`#eee` — the only gray left is `button:disabled`'s `#666`, which is supposed to look inactive. "Grið" → **"Reitir"** everywhere user-facing (the settings-popover label and the `⊞`/`✋` toggle button text; `settings.grid`/`#grid`/`toggleFreeMove()` etc. keep their internal names). **🖨 Prenta moved behind a `⋯` overflow menu** (`#moreBtn`/`#morePop`) next to "Núllstilla röðun" (also relocated there — same "rare/office action" logic). **Popovers are now robust and always closable**: `⚙ Stillingar` and the new `⋯` share `togglePopover()`/`closeAllPopovers()`, each has an explicit `.pop-head` **✕ close button** (never rely on tap-outside alone), and the outside-dismiss listener moved from a `click` handler to `pointerdown` with `capture:true` — fires before the board's own drag-start handlers and behaves the same on touch as on a mouse, fixing the popover that could get stuck open on a tablet.

**Floor-specific polish (2026-09-12)**, ahead of `/saga` going into real use: `grain.html` now loads **Kumbh Sans** (font-family swap only, ties it visually to `velja.html`/`skipulag.html`/`verkstaedi.html` — no layout changes). `body.floor` gets its own touch-target bump — `button.small` and the guide's `.gboard-nav` pills go from `5px 9px`/`4px 10px` up to `9px 14px` — since a shop tablet with sawdust/gloved fingers needs bigger hit targets than an office mouse pointer; office density is untouched. The floor's "▶ Byrja sögun" plan-bar button is now amber (`#ffd60a`, matches the Saga card on `/verkstaedi`) instead of the default blue, so the hub→guide hop reads as one product. **Floor's edit toolkit was trimmed to true "nudge" actions**: `⎘ Afrita` (duplicate), `＋ front` / `＋ Nýr stakur front` (add material), `✂ skipta upp` (split a run), the "sameina við…" merge select, and a manual group's width input are now `office-only` — those change *what* gets cut, not just *where*, so they were always meant to be office-only per the "floor can nudge" docs above but weren't actually gated. Reorder (↑↓, ↕ snúa röð), orient (v/s/h), drag, send-to-board, and the `⚠` flag-a-problem button stay floor-visible.

**Label printing from the guide (2026-09-14)**: user's next ask — "print labels when the piece is finished or print all the labels on a sheet." Found the local copy of `print-server.js` (shop PC, not in any repo — `~/Downloads/Björninn/Sögunarlisti & Kerfi/print-server.js`) and read it in full before designing anything: it polls Airtable every 5s for any Sögunarlisti row with `B.A.S.` checked, prints a label straight from that row's own standard fields (Partur/H/B/Þ/Kantlíming/Magn/project, all read by **field id**, not name), tracks per-row print-count itself in a local `counts.json` (not an Airtable field), and unchecks `B.A.S.` when done (setting `Lokið` once the last copy — per `Magn` — has printed). Critically: **it doesn't care what set the flag** — `labels.html` already uses exactly this mechanism, so grain.html just needed to trigger the same flag, no new label format or server-side change required. `B.A.S.`/`Lokið` were already in `api/airtable.js`'s Sögunarlisti allow/writable lists (added for `labels.html`), so no proxy changes either.

Added `printOneCycle(pieceId)` (PATCH `B.A.S.=true`, poll every 500ms up to 20s for it to clear or `Lokið` to flip, mirrors `labels.html`'s own `printOneCycle` closely enough to be recognizable), `printPieceLabel` (single piece, called from a new 🖨 button on each real produces-chip in the guide) and `printBoardLabels` (batch — every real, not-yet-`Lokið` piece across the current board's steps, one full cycle per physical copy since a `Magn > 1` row needs one press per copy same as `labels.html`). Manual fronts (no `pieceId`) get no button at all, per explicit instruction ("we will probably not use this function a lot" — skip building anything for the rare case). `_printedPieces` (session-only `Set`, mirrors the existing `_flaggedPieces` pattern) greys a chip out with 🖨✓ once confirmed `Lokið`.

**Testing note worth remembering**: `printOneCycle` PATCHes a real, live Sögunarlisti record — if the shop's actual `print-server.js` happens to be running and polling the same shared Airtable base, testing this for real would print a genuine physical label on the shop floor and waste it (or confuse whoever's there). Verified the entire flow — single print, last-copy/`Lokið`, timeout/server-offline, and the batch path across a real board's 16 produces-entries (6 unique pieceIds, confirming one cycle per physical copy, not deduped by row) — by temporarily monkey-patching `airtablePatch`/`airtableFetch` in the browser console to a mock that never touches the real network for `SOGUNARLISTI_TABLE`, then restored them with a reload before ending the session. No real Airtable writes happened during testing.

**Band-first sequencing — verified across every real project (2026-09-15)**: user asked to "triple check nothing is missing and it actually works, try on other projects" after the band-first round below. Ran the full verification suite (crosscut/rip strict-monotonicity, a full guide walkthrough for render errors, and a new front-coverage check — every front in `groups` must be produced exactly once across the whole plan, via `gid`+`fi` matching against `produces` entries) against **all 15 melamine-eligible projects** in the base (T-33, T-15, T-49, T-54, T-56, T-58, T-113, T-70, T-30, T-80, T-90, T-92, T-126, T-132, T-189 — via `loadProject()` per project, not just a page reload, so each got a genuinely fresh `buildGroups()`+plan load). T-24 and T-31 were correctly excluded — checked their `allFronts`, both are real wood veneer (Spónlagt), not melamine, which `/aedar` deliberately excludes (no book-matching support) — not a bug. **Result: zero real problems across the board** — every project's front-coverage check came back 0 missing / 0 duplicated, zero render errors walking every step, and confirmed band-mode is genuinely exercised (not dormant) on multiple projects, not just T-33.

Two things surfaced and resolved during this pass, both false trails rather than real bugs in the band logic: (1) the first version of the rip-monotonicity check flagged T-54's "narrower" rip steps (a run's own trim-to-width, which happens *after* that run's own crosscuts already separated it from its strip/column-mates) as going backward — these are independent operations on already-separate pieces once crosscut apart, so comparing their positions against each other is meaningless; refined the check to exempt title-matched `"rífðu N fronta niður í"` rip steps from the monotonicity requirement, confirmed by hand-tracing T-54's actual steps that the sequence was correct all along. (2) T-70 showed real crosscut/rip anomalies and 6 real `detectOverlaps()` hits — but this was **pre-existing corrupted data in T-70's own saved draft** (same class of issue T-33 had before this session's earlier reload-corruption fix), not a band-mode bug: a clean `buildGroups()`+`autoNest()` rebuild (same recipe used on T-33/T-15) immediately came back with 0 overlaps and a fully clean cut-plan on all 4 of its boards (all band-mode). That rebuild persisted (autoNest() always saves) — T-70's stale overlap got fixed as a side effect of verification, same pattern as T-15's cleanup two rounds ago.

**Band-first cut sequencing for multi-group strips (2026-09-15)**: user spotted a real, concrete problem while looking at board 15's first step: the guide's first cut was a full-length (2790mm) vertical rip that passed through the Y-space of 5 stacked, unrelated groups just to reach the one at the top — "instead of going horizontal to 'free up' the one we are going after." Confirmed via a physical fact: a crosscut always spans the full width of whatever's currently on the saw (unlike a rip, which can be any length), so "crosscut a row off first" for a column-packed board necessarily means cutting across the *entire remaining sheet width*, not just one strip — a fundamentally different cut-sequencing mode from the existing one (rip full-length strips first, then crosscut each strip), confirmed with the user before building given the size. Implemented: `useBandFirst = strips.some(s => s.runs.length > 1)` — per board, if ANY strip (column) currently holds 2+ unrelated groups stacked in it, the WHOLE board switches to band-mode: crosscut a full-width band down to just what the next group needs (via a proper Y-interval-merge in the new `clusterBands()`, not naive proximity clustering — see below), then rip that already-short band into its groups. Boards where every strip holds exactly one group are untouched (rip-first already gives the shortest cut there — the majority case). Extracted the per-run crosscut/rip sequencing (a run's own fronts, or its wing/rotated-band treatment) into a shared `emitRun()` used by both modes, so front-separation logic is identical either way — only how a run's own workpiece got isolated from the sheet differs.

**Two real bugs found and fixed while building this** (same "trace every step's cut position, assert strictly increasing" method from the overlap-reload investigation above): (1) `clusterBands()`'s first version mirrored `clusterStrips()`'s X-clustering directly — cluster by how close two items' start Y is. This is unsound: column-first packing only guarantees non-overlapping *columns*; what each column stacks along Y internally is independent, and different columns' stacks routinely interleave — caught live on T-33 board 12, where an item 1254mm below another item's *start* Y still had its whole span sitting entirely inside that other item's already-cut region (a band's crosscut computed *before* another item's own band, at a smaller Y — the guide would have told the sawyer to cut material that was already gone). Fixed by properly merging overlapping Y-intervals (the classic flatten-overlapping-ranges algorithm) instead of proximity clustering — two items merge into one band iff their Y-spans genuinely overlap, regardless of how far apart their start positions are; band membership then propagates transitively (merging a growing band can bring it into overlap with an already-built one, so the merge loop repeats until stable). (2) Multiple runs sharing the *same* X position within one band (stacked vertically, exactly like an ordinary strip) were each getting their own "rip to width" step at the identical cut position — asking the sawyer to make the same cut twice on two different physical pieces. Fixed by extracting `clusterByX()` out of `clusterStrips()` and reusing it *within* each band to find these same-column runs first, so they share one rip (mirroring exactly how a normal multi-run strip already works), then crosscut sequentially.

Verified thoroughly on T-33's real 18-board, 320-step plan: 122 crosscut sequences and 52 rip sequences all strictly increasing (the literal "never ask for material already cut away" check), zero render errors walking every single step, all 13 drawer groups (from the earlier keepTogether/ordering fix) still correctly ordered under band sequencing, the "hide already-cut pieces" guide feature (below) still correct, and a spot-checked simple (single-group-per-strip) board's step sequence completely unchanged from before this round — confirming no regression for the majority case.

**Overlaps kept reappearing on every load — the real root cause (2026-09-15)**: user asked to fix the 9 overlaps flagged on T-33's boards 9/13/17 last round. First fix attempt (`autoNest()` + save + verify clean on reload) *worked* — until the NEXT reload, when the exact same 9 overlaps came back, with nothing edited in between. Traced it fully: `saveLayout()` only ever wrote `{layout, board}` to localStorage, never `groups`. `autoNest()` can split a non-`keepTogether` run into single-front pieces to fit better (round 6's yield fallback), each under its own `${gid}~${frontId}` id — so a load that takes the `stored.layout` + `reconcileLayout()` path (this device's local edits, or the office's own browser with anything cached) pairs that *possibly-split* `layout` with a freshly-`buildGroups()`-rebuilt `groups` — always **unsplit**, since only `autoNest()` ever splits, never `buildGroups()` itself. `detectOverlaps()`/`unplacedGroups()` both silently skip any placement whose id has no matching group (`groups.find(...)` returns undefined → filtered out), and `reconcileLayout()` read an already-split, fully-placed run as "missing" and stacked a **second, fresh, unsplit copy** of it onto the last board on top of whatever was already there — the actual mechanism, confirmed via direct instrumentation (`console.trace` in `reconcileLayout`/`settleBoard`, checked exact group counts: `buildGroups()` alone gives 91 groups on T-33, `autoNest()` deterministically splits some down to 108 — every single repeated `autoNest()` run gave the identical 108, split identically, so the packer itself was never the culprit). Fixed at the source: `saveLayout()` now persists `groups` alongside `layout`, and `selectMaterial()`'s local-storage branch restores them *together* — never lets a split `layout` pair with an unsplit `groups` again. Also hardened `reconcileLayout()` itself to compare by **front identity** (`frontId()`: uid/mfId) instead of raw group id — so even a genuine split/unsplit mismatch (e.g. from an old pre-fix localStorage save) can't misread a still-valid fragment as gone — and to only call `settleAll()` when something is *actually* missing, since replaying `nearestFree()` on an already-valid, tightly-packed board for no reason risked shifting a run by a hair and leaving it nowhere valid to go. **First attempt at the `reconcileLayout()` front-identity check had its own bug** (naive `gid.slice(lastIndexOf("~")+1)` grabbed the wrong suffix, since `frontId()` itself already contains a `~` in `pieceId~i`) — caught by the same reload-stress-test methodology, fixed by building an exact `${originalGroupId}~${frontId}` → frontId lookup instead of reverse-parsing. Verified with 5+ consecutive fresh reloads on both T-33 and T-15 (including from a clean Airtable-only load, no localStorage at all): 0 overlaps, 0 unplaced, stable group count every single time — previously this was reproducibly broken on nearly every reload. T-15's own long-standing pre-existing mess (26 unplaced groups, dozens of overlaps, noted back in round 4 and never touched since) got fixed as a side effect of this verification — not explicitly asked for, but leaving it broken after discovering it mid-test made no sense.

**Guide diagram now hides already-cut pieces (2026-09-15)**: user asked, while testing the cut order fix from last round, whether the guide's board diagram could stop showing pieces that have already been cut off and labeled — "to not confuse the sawyer." Confirmed the concern was real: `guideSvg()` drew every group's full original footprint on every step regardless of progress, so a sawyer several steps into a board still saw a piece already separated and set aside as if it were still part of the remaining sheet. Fixed: `produces` entries in `buildCutPlan()` now carry `gid`/`fi` (which exact front, by array position, since cuts always happen in front-array order so "done" is always a contiguous prefix — never a done front with an undone one ahead of it). `guideSvg()` now builds a "done" set from every step *before* the one on screen, and for each placed group either skips it entirely (every front already cut) or shrinks its drawn footprint to just the still-uncut remainder, skipping individual front outlines that are already gone. Verified precisely on a real 3-front drawer stack ("Sk 19,21 · 1", heights 200/240/270): at each successive guide step, exactly the right front's rectangle disappears from the diagram, in the correct order, never early or late (checked exact rect coordinates, not just counts). Full 292-step walkthrough of T-33's real guide renders with zero errors.

**Drawer fronts: keepTogether + real-height sort (2026-09-14)**: user reported "Fjöldi eininga is 2 or more" projects (a Eyðublað unit representing 2+ duplicate physical cabinets) produce drawer-front stacks that "don't get arranged correctly" — asked for the rule "smallest at the top, biggest at the bottom, shrinking as you go up." Found two real bugs in `buildGroups()`: (1) drawer-front runs (`Skúffufrontur`) never got `keepTogether:true` — only wing/split fronts did — so `autoNest()`'s yield-splitter and `normalizeGroups()`'s overflow fallback (round 6/round 9's mechanisms) could freely scatter a drawer bank across sheets once Fjöldi eininga made the combined run long enough to trigger them; confirmed *live* in T-33's own currently-saved draft — a real "Sk 26-27" 6-front drawer stack (2 duplicate cabinets × 3 drawer heights) was already split into two disconnected `[K,E]` fragments with `keepTogether:false`, plus one of its fronts had drifted into an unrelated group. (2) the front sort was drawer-code-letter-first (`CODE_RANK`, N→E "narrow→tall") with a **descending**-height tiebreak — backwards from "smallest on top" whenever two fronts shared a code or (more commonly) the Partur text had no embedded code letter at all (plain `"Skúffufrontur - Merivo"` rows, no N/M/K/C/D/F/E — checked real data: rare project-wide but 100% inverted when it happened). Fixed both: added `DRAWER_RE = /skúffufrontur/i` alongside the existing `SIDEBYSIDE_RE`/`KEEPTOGETHER_RE`, sets `keepTogether:true` + `kind:"skúffur"` same as wing/split fronts; replaced the sort with **height-ascending as the primary key**, code rank only breaking a genuine tie between two equal-height fronts — robust regardless of whether a code letter is present, and doesn't depend on Blum's code order actually matching a project's real measured heights (found one real example, "Sk 11-13", where E and K fronts had the *same* H — code order alone would've been ambiguous there too). Verified via real data: queried every non-inner `Skúffufrontur` row base-wide (210 rows) to confirm the codeless case is real but rare (4/210, isolated to one old project); loaded T-33 live and inspected `groups` directly — all 9 drawer runs now `keepTogether:true`, `kind:"skúffur"`, front order ascending by H; ran the real `packGuillotine()` (the function `autoNest()` calls) against a snapshot of T-33's real groups for both packing strategies — zero drawer runs split, missing, or reordered in either. **Flagged to the user, not fixed by me**: T-33's live *draft* (unconfirmed) still has the old fragmentation baked in — `applyPlan()` restores a saved plan's groups verbatim, so loading the code fix alone doesn't retroactively repair a plan that was already saved broken; T-33's current draft also has 382 `detectOverlaps()` overlaps (pre-existing, unrelated to this fix, likely from repeated `checkNewFronts` additions over the long session). Recommended the user re-run "Núllstilla röðun" (or equivalent full rebuild) on any drawer-front project to pick up the fix — not done automatically, since forcing a live draft rebuild without asking risks discarding in-progress manual arrangement work.

**Drawer fronts, follow-up — split per cabinet, not per unit+width (2026-09-14)**: user pushed back on the previous round's fix immediately: keeping the *whole* unit+width bucket together as one atomic run was unnecessary and wrong — it merged 2 duplicate cabinets' (Fjöldi eininga ≥ 2) drawer fronts into one interleaved 6-front stack (M,M,K,K,E,E), which breaks grain continuity within a single *installed* drawer bank (a cabinet's own 3 drawers are no longer cut from 3 consecutive positions in the strip) and needlessly forces both cabinets to live on the same sheet. Asked for: keep each cabinet's own drawer set together (reset per unit, "(L→M→S)→(L→M→S)"), but let different cabinets' sets land independently if that packs better. Fixed in `buildGroups()`: `Skúffufrontur` rows are now keyed `${unitId}|${width}|${i}` (the row's own copy index `i`) instead of just `${unitId}|${width}` — pairing "copy #i of every drawer-height row sharing this unit+width" into its own group. This is always a physically valid reconstruction (same-height copies are 100% interchangeable, Sögunarlisti just doesn't track which duplicate cabinet each belongs to) and degrades gracefully when one drawer height's `Magn` is shorter than its siblings' — those extra copies just form their own smaller trailing group instead of guessing at a wrong pairing (verified against a real mismatched-Magn case, "Sk 1,3": N Magn=2, E Magn=4 → naturally becomes 2×[N,E] + 2×[E]). Plain (non-drawer) fronts are untouched — still grouped by unit+width only, since their `Magn` means "N identical copies," not "N drawer positions." Groups from a multi-cabinet unit get a "· 1"/"· 2" suffix on `unitName` for panel legibility; a single-cabinet unit's group keeps its plain name (no suffix noise). Verified against T-33's real data again: "Sk 26-27" (2 cabinets × 3 drawers) now produces 2 separate `[M,K,E]` groups instead of 1 combined 6-front block; re-ran the same `packGuillotine()` intact-check as the previous round (both strategies) — 14 drawer sub-groups (was 9 combined groups), zero split/missing/reordered, each cabinet's own stack free to land on whichever sheet fits.

**Guide banner text pass (2026-09-14)**: user's Icelandic feedback — "má ekki vera svona mikill texti... helst bara 2 orð per leiðbeiningu" (must not have this much text, ideally just 2 words per instruction) — on the `reorient`/`switchPiece` banners, which had grown into full sentences explaining what to do. Cut both down to a bold 2-word headline (`🔄 Snúðu efni` / `↔️ Nýr biti`) with a smaller `.gsub` subtitle span (`Ný átt` / `Sama átt`) underneath — same information density a sawyer actually needs mid-cut, no paragraph to read. `.gsub` needed a new CSS rule (`display:block; font-size:0.8rem; ...`) since `.greorient` had no existing style for a two-line headline+subtitle layout — bumped `.greorient` itself to `1.15rem`/`800` weight so the headline reads as a real instruction, not body text. Verified by injecting the exact banner markup into a live `#guideOverlay` (removing its `.hidden` class) rather than driving a full project through the guide flow — faster and just as valid for a pure CSS/markup check.

**WinCut guide, legibility pass (2026-09-14)**: user's first reaction to the new panel — remove the now-redundant `.gdatum` line (the old "frá síðasta þverskurði... fastur stoppari: N mm" text; the WinCut Type/Value/Rep table already gives the exact number to type, so the older prose-plus-alternate-number was pure noise on top of it) and the per-step `.gsheetlen` reminder (they'd already internalized "set it once," repeating it on every single step was noise too) — both removed outright, along with `buildCutPlan()`'s now-unused `datum`/`absStop` computation (nothing else read it). Asked for the WinCut table itself to be bigger and white — shop-floor legibility, this is the one thing on screen the sawyer actually needs to act on. `.gwincut-table` went from 0.85rem muted blue-gray to 1.3rem pure white, the current line to 1.5rem/800-weight; verified via computed style, not just the source (`rgb(255,255,255)`, `24px`/`800` on the active row) — same lesson as the popover cascade bug from two weeks ago, don't trust the CSS you wrote until you've checked what actually rendered.

**WinCut F2 integration — guide now speaks the real machine's language (2026-09-14)**: user sent real photos of WinCut's "SEMI-AUTOMATIC" screen (very likely what F2 opens — it's the second item on the main-page function list in the manual, right after Manual Commands). Confirmed directly from the photos + the user: a program is a flat list of `Type` (`XA` = a non-cutting reference — "just the width of the sheet you have", `Cut:0`; `XI` = an incremental cut from the *previous* cut line, `Cut:1`) / `Value` / `Rep.` (repeat count — a real program had `568.000, Rep 2` for two identical cuts back to back) / `Cut` / `Wid.` rows, and **every row in one program is the same axis** — there's no way to mix a rip and a crosscut in one WinCut program, confirming the "only one direction" constraint literally. `Sheet length` is typed by hand once per physical sheet; asked the user whether to also update it between every cut (technically more correct) or leave it at the full value all the way through — agreed to leave it: fewer manual entry points, each one a chance to mistype under time pressure, for a small and predictable cutting-speed cost.

Implemented `buildWinCutPrograms(steps)` (called from `buildCutPlan()`, attached as `plan.programs`): walks the existing step list and groups consecutive steps into WinCut-program blocks, collapsing identical consecutive `measure` values into one line with a `Rep` count exactly like the real screen. `renderGuide()` now shows, alongside the existing step info: a reorient banner when the next program is a different axis ("🔄 snúðu/matið efnið"), a persistent `Sheet length` reminder, and the program's own Type/Value/Rep table with the current line highlighted (feedWidth/`XA` value shown too, flagged with a ⚠ on rip-axis programs since that specific mapping — width vs length for the declare value — isn't confirmed against a real rip-mode photo yet).

**Bug caught during testing, before shipping**: grouping purely by (board, axis) is wrong — a rotated ("h") group's per-front crosscuts off an already-separated "band" are `"v"` axis, and so is the *next strip's own rip*, but they are not the same physical piece of material. The original grouping merged them into one program anyway. Root-caused precisely (traced steps 2/3/4 of a real plan: steps 2-3 correctly shared a program on the band, step 4 wrongly joined them despite being a cut on the main sheet). Fixed by adding a `workpiece` id to every step in `buildCutPlan()` (`${bi}-sheet` for the evolving sheet remainder between strip rips, `${bi}-strip${si}` for a strip once separated, `${stripPiece}-band-${g.id}` / `${stripPiece}-tall-${g.id}` for a rotated band or a wing group's "tall" piece once separated from its strip) and having `buildWinCutPrograms` break on workpiece changes too, not just axis. Added a second transition type for this — `switchPiece` (same axis, different piece: "↔️ skiptu um verkstykki — leggðu frá þér...") — distinct from `reorient` (axis changed, physically rotate the material), since telling a sawyer to "rotate" when nothing needs rotating would itself be a real mistake to ship. Verified thoroughly on both real projects: stepped through *every* step of the full plan (T-33: 265 steps / 164 programs / 139 reorients / 9 switch-piece; T-15: 181 steps / 108 programs / 96 reorients / 4 switch-piece) with zero render errors, and spot-confirmed the exact bug scenario (steps 2/3/4) resolved correctly after the fix (step 4 now gets its own program with `switchPiece: true`, not merged into the band's).

**Two things flagged, not yet acted on**: (1) the reorient count is high — roughly half of all steps on both projects trigger a physical rotate/re-feed. Worth a conversation with the user about whether nesting should also weigh "how many WinCut program switches does this produce" alongside sheet-count/yield, since those switches are exactly where operator time and mistake risk concentrate — not attempted this round, this round was about making the existing plan legible in WinCut's own terms, not re-optimizing for it. (2) T-33's live saved state currently has a real, pre-existing group-overlap warning (`Hópar skarast`) — unrelated to anything touched today (no `autoNest()`/`settleBoard()` changes this round), first noticed while testing the guide; needs the same kind of investigation as the earlier settleBoard bug did, just not done yet.

**Dual-strategy guillotine packer (2026-09-14)** — the user asked for this to be bulletproof given real material cost is on the line, so it got design sign-off, thorough pre-implementation planning, and simulation-based verification against two real projects before ever touching a live saved plan. `autoNest()`'s old column-only model is a valid 2-stage guillotine sequence (rip into width-based columns, crosscut within each), but it can only ever organize a sheet by shared **width** — a short, very wide run (Sökkulcover) that would rather share a row with other short pieces has no way to do that, no matter how the column search is patched (round 5-8's history). Rejected two more ambitious alternatives before this one: full 2D free-rectangle (MaxRects) packing can produce layouts that aren't achievable by any sequence of straight guillotine cuts — i.e. geometrically valid but **physically uncuttable** on a beam saw, which a pure column model can never do by construction; metaheuristics (simulated annealing etc.) trade predictability for yield, wrong tradeoff for "bulletproof." Landed on: extract the column logic into a generic `packGuillotine(groupsIn, primaryLimit, secondaryLimit, primaryOf, secondaryOf)` that packs in "primary/secondary" strip-space (a strip's *size* along the primary axis, *used* along the secondary), then run it **twice** — once as `primary=width` (the original "column-first"), once as `primary=length` (new "row-first", the literal mirror: strips run the sheet's width instead of its length, letting different-width pieces that share a *length* sit side by side) — and keep whichever used fewer sheets (yield as tie-break). `autoNest()` itself is now a thin wrapper: run both, pick the winner, map its `{primary,secondary}` positions back to real `{x,y}` (`{x:primary,y:secondary}` for column-first, transposed for row-first), then the same `offsetPlace`/`saveLayout`/`render` as before. Split-fallback and shared-floor/bottom-band logic (rounds 6 and 8) both moved into the generic function unchanged — they're already dimension-agnostic given primary/secondary instead of hardcoded width/length. Verified via pure, non-mutating simulation (calling `packGuillotine` directly against a `buildGroups()`-fresh snapshot, never assigning to the live `groups`/`layout` or calling `saveLayout()`) on two real projects before running the real function on anything: zero overlaps, zero duplicate ids, exact front-count preservation, and — reusing the exact repro that caught the round-9 duplicate-fronts bug — 10 repeated simulated re-runs on each project with nothing wrong on any pass. Confirmed row-first is doing real work, not just theoretically: on T-33, Sökkulcover went from sharing a sheet with 6 other groups (column-first) to 10 (row-first) — total sheet count still tied at 18 on that specific project (row-first's win there is offset by a loss elsewhere), and the repeated-run test organically found 17 by pass 9 as a side effect of the extra flexibility from repeated splitting. T-15 held steady at 9 boards / 78.9% either way.

**Critical fix — repeated auto-nest could silently drop fronts (2026-09-14)**: user reported that clicking "↻ Raða sjálfvirkt" a few times in a row made Sökkulcover fronts disappear with no warning. Confirmed and reproduced: `autoNest()`'s split-fallback (added when auto-nest gained the ability to split a non-`keepTogether` group across sheets) gave each split piece the id `${g.id}#${i}`, where `i` was the front's position in whatever array it happened to be iterating at that moment — not stable. On a second `autoNest()` call, the "leftover" remainder of an already-split group (which keeps the *original* group id) can itself get split again, and its own (now smaller) fronts array restarts indexing from 0 — producing an id that collides with a piece created by the *first* split. `groups` ends up with two different group objects sharing one id; `layout.boards[*].placements` is a plain object keyed by id, so the second write silently overwrites the first, and one of the two fronts never gets a board position — invisible on every sheet, absent from the cut plan, and **not** caught by `unplacedGroups()` (which only checks whether an id exists anywhere, not whether it's unique). Reproduced on live data: 10 repeated auto-nest calls on a real project produced up to 17 duplicate ids per pass. Root issue went one level deeper than expected — the obvious fix (key split-piece ids off each front's `pieceId` instead of array position) *also* collides, because `pieceId` is the Sögunarlisti **row's** id: `buildGroups()`'s `for (let i = 0; i < p.m; i++)` loop pushes `p.m` separate front objects that all share the same `pieceId` when a row's magn/quantity is more than 1 (exactly Sökkulcover's case — one row, several physical pieces). Real fix: `buildGroups()` now stamps each front with its own permanent `uid` (`${p.id}~${i}`, the loop index baked in once and never regenerated) at creation time; `frontId(f)` (new helper) returns `f.uid || f.mfId` and is now what all three split-id call sites (`normalizeGroups()`'s overflow fallback, `splitGroup()`'s manual "✂ skipta upp" button, and `autoNest()`'s split-fallback) key off, instead of array index. `serializePlan()` was missing `uid` from its per-front whitelist — added, or the field would vanish on every save/reload. Plans saved *before* this fix have fronts with neither `uid` nor `mfId`; `applyPlan()` now backfills a synthetic `uid` (`legacy~N~<random>`) onto any such front the first time an old plan loads, so it only ever needs to happen once per project (the next save carries the real one forward). Verified thoroughly before shipping: 10 consecutive `autoNest()` calls on real data, zero duplicate ids/duplicate placement keys/unplaced groups/overlaps every single pass (was failing by pass 1, worsening through pass 10, before the fix); separately verified the legacy-plan backfill path the same way, starting from a synthetic pre-`uid` plan. **This is the kind of bug that costs real material if it ships wrong** — a front silently missing from the cut plan means a physical part never gets cut, discovered only once the cabinet can't be assembled. Given that stakes, don't trust "duplicate id" bugs to `unplacedGroups()`'s coverage — it was never designed to catch id collisions, only missing placements.

**Office round 3 — melamine-only project list, group lock, sheet collapse (2026-09-12)**, a 6-item batch from the user: (1) **Project dropdown now hides projects with no melamine front material** — `populateProjects()` populates the full unfiltered list immediately (fast), then `filterProjectsByMelamine(recs)` runs a background cross-project Sögunarlisti query (same batched `OR(FIND(...))` pattern as `annotateProjectsWithPlans`) and prunes non-matching `<option>`s in place once it resolves; deliberately non-blocking after an early attempt awaited it before populating anything and left the picker stuck for 2+ minutes locally (the query scans every Sögunarlisti row across all eligible projects — a lot of data even before the front/melamine filter runs client-side; local `vercel dev` pagination is known-slow, prod should be much faster). (2) **`.mat-photo` grew from 40px/9px-radius to 64px/11px-radius**. (3) **`#planStrip` (the "Drög/staðfest" bar) moved inside `#projectCard`**, right under the header pill instead of sitting as its own bar above the toolbar. (5) **Removed the `.hint` block under "Æðahópar"** ("Á plötunni: dragðu ⠿-handfangið...") — the user called it pure noise now that the UI is otherwise self-explanatory. (4) **Group lock** — `g.locked` (persisted, `serializePlan`/`applyPlan`) with a 🔒/🔓 toggle button (`toggleLock(gid)`) in the panel, first item in `.grp-actions`, only shown when the group is selected; every mutation path that could add/remove/reorder fronts into or out of a locked group is guarded: `addFrontToGroup`, `splitGroup`, `enableFrontDrag`'s pointerdown (SVG run-drag), `frontDropTarget`/`maybeMergeOnDrop` (drop targets skip locked groups), `mergeCandidates`/`mergeGroup`, `dropFrontRow` (panel HTML5 drag). The whole-run ⠿ drag (repositioning on the sheet) still works on a locked group — locking only stops *membership* changes, not moving it around. Board SVG header band shows a 🔒 prefix; panel `<h4>` shows a 🔒 with a tooltip; locked groups get a dim gold border (`.grp.locked`), brighter when also selected. (6) **Sheet/board lock ("Læsa plötu")** — `b.done` (persisted) hides a finished board from the main grid and rolls it into a `.done-pills` strip at the top (`🔒 Læstar plötur (N)`, each pill shows its m² used + a `↺` to reopen); `toggleBoardDone(bi)` is pure view-state, never touches `placements`/`groups`, so stats/plan JSON/guide are unaffected. Shared `boardUsage(b)` (extracted from the old per-board used/waste calc) backs both the full board head and the collapsed pill. Guarded so nothing can silently land on a hidden board: `sendToBoard`, `removeBoard`'s orphan-relocation, `reconcileLayout`, and `addNewFronts` all auto-un-hide (`done = false`) the target board before placing onto it — `reconcileLayout` in particular now searches backward for the last **non-done** board (`[...layout.boards].reverse().find(x => !x.done)`) instead of always grabbing `layout.boards[length-1]`, since the last board could be a locked/done one. **Caught and fixed during verification**: extracting `boardUsage()` out of `renderBoards()` accidentally dropped the local `const placedGids = Object.keys(b.placements)` that a later line in the same loop (the per-front SVG render) still depended on, throwing "placedGids is not defined" and blanking every board — fixed by inlining `Object.keys(b.placements).forEach(...)` at that call site instead of relying on the now-removed outer variable. Verified via a full synthetic pass in the live browser (T-15, real data): lock/unlock toggling the UI and blocking every guarded mutation path, board collapse/pill/reopen, `sendToBoard` auto-reopening a done board, and a `serializePlan()`→`applyPlan()` round-trip correctly restoring both flags.

**Office round 8 — reverted auto-rotation, added a real bottom-band fix instead (2026-09-14)**: user corrected round 7's rotation-trial addition immediately — they never asked for automatic rotation ("I said nothing about orientation. Height should always be vertical. unless manually flipped."), so `tryPlaceExisting()`/`placeOnFreshSheet()`'s orientation-swap attempt was removed entirely; `autoNest()` now uses a group's orientation exactly as assigned again, same as before round 7, and never touches it. What they'd actually pointed at (a screenshot of "Plata 12": three 596mm-wide single-front columns, all the same 2587mm height, with an obvious ~200mm gap across the full width below all three) needed something different: a Sökkulcover run (2050mm wide, 80mm tall per front) can't join any of those columns (too wide) and can't become its own new column either (not enough leftover sheet width beside them) — but the space below all three, spanning their combined width, genuinely is free, and nothing in the column model could see that, since each column only tracks its own narrow width independently. Added a **bottom-band** fallback to `tryPlaceExisting()`: after the normal per-column and new-column checks fail for a sheet, compute `floorY = max(col.used for every column on that sheet)` — the y below which *every* column has already finished — and if the candidate fits there across the sheet's full usable width, place it at `(0, floorY)` and bump every column's `used` up to match (so the next item's search sees the new shared floor correctly, and a second short item can stack further below the first). Verified with a synthetic reproduction of the exact screenshot geometry (three 596mm columns each used to 2591mm) before trusting it: a synthetic 2050×80 Sökkulcover piece landed at exactly `(0, 2591)`, as expected. A cold `autoNest()` rebuild of T-33 doesn't happen to reproduce that specific three-equal-column shape from scratch (the widest-first greedy pack constructs sheets differently), so this round's board count held at 18 rather than dropping — the mechanism is confirmed real and will activate whenever that column pattern occurs, whether during auto-nest or after the sawyer/planner has arranged a sheet into that shape and reruns "↻ Raða sjálfvirkt".

**Office round 7 — real settleBoard bug found + auto-nest tries rotation (2026-09-14)**: user asked me to look at a live project (T-33) after manually rotating a few groups 90° by hand and noticing sheet 12 had room a rotated Sökkulcover run could use. Checking that live state surfaced something much more urgent than the packing question: **T-33's saved draft had ~57 pairs of genuinely overlapping groups**, almost entirely piled onto one board (18) that had **173% of its area assigned to it** — 14 groups where roughly 8 fit. Root cause: `settleBoard(bi)`'s collision resolution only ever pushed a run straight **down** past whatever it hit, and when that push would run it off the bottom of the sheet, it clamped back up **and gave up right there without re-checking** — silently leaving it overlapping whatever was already there instead of trying anywhere else (sideways, say). A run of manual rotations (each `setOrient()` call triggers `settleAll()`) kept hitting this same dead end and never actually resolved, so groups just kept accumulating unresolved on top of each other. **Fixed properly**: `settleBoard()` now clears the board's placements and re-adds each run (in the same y/x reading order as before) via `nearestFree()` — the same free-space search drag-and-drop already uses (edge-butt candidates in both axes, then a coarse grid-scan fallback) — instead of its own weaker one-directional logic. Confirmed the fix works and confirmed the underlying diagnosis simultaneously: calling the new `settleBoard()` directly on the broken board did *not* move anything (nearestFree correctly reported no free space — the board genuinely had ~2× its capacity assigned, not a search bug), while running a full `autoNest()` afterward correctly redistributed everything back to **18 boards, 0 overlaps, 0 unplaced, 81.4% yield** — same board count as before, now actually valid. **Also, the actual question asked**: `autoNest()` previously froze a group's orientation at whatever `buildGroups()`/`normalizeGroups()` assigned once and only ever tried that single footprint when searching for a fit — exactly why the user had to rotate pieces by hand to unlock better placements. `tryPlaceExisting()` (and `placeOnFreshSheet()`) now try both the group's current orientation and its 90° swap (v↔h) before giving up, for the same non-`keepTogether`/non-locked groups the splitting logic already applies to — mutating `g.orient` to whichever one actually fit. Re-running `autoNest()` on T-33 post-fix rotated 13 groups to "h" (up from the user's 6 manual ones), confirming the algorithm now finds rotation opportunities a human would otherwise have to spot by eye. **Lesson for next time a "let's optimize this further" conversation touches a live project's saved plan: check `detectOverlaps().length` before anything else** — an optimization discussion nearly proceeded on top of an already-broken layout.

**Office round 6 — auto-nest can now split non-grain-critical groups across sheets (2026-09-14)**: follow-up to round 5's width-first sort, after the user pointed at a real example (T-33) where a "Sökkulcover Eldhús" run sat as one 6-piece block instead of letting individual pieces backfill gaps elsewhere, and asked whether some "tegund" should be freely splittable and some not. Answer: reuse the *existing* `keepTogether` flag (already correctly marks væng/skiptur pairs that must stay grain-continuous) as the only thing `autoNest()` treats as atomic — everything else is fair game to split at the individual-front level if that's what it takes to avoid opening a new sheet. Explicitly asked the user how to weigh yield against the extra rip cuts a split group needs (it loses its shared strip cut); they chose favoring material savings now, cutting-sequence cost to be dealt with once the WinCut/F2 work starts. Implementation: `autoNest()`'s per-group placement now has a fallback — if a whole (non-`keepTogether`, non-locked, multi-front) group doesn't fit *any already-open* sheet, each of its fronts is tried individually (`${g.id}#${i}` ids, same convention `normalizeGroups()`'s own overflow-fallback and the manual "✂ skipta upp" button already use) against existing sheets **before** a new sheet is opened; whatever's left over stays as ONE group (no point fragmenting further than the gaps actually need) and only that remainder opens a new sheet if it must. `groups` itself is now reassigned at the end of `autoNest()` (`groups = finalGroups`) since splitting changes what groups exist, not just where they sit — this is new, `autoNest()` previously only ever repositioned existing groups. **Verified against T-33's real 84-group data, and the finding matters**: board count stayed at 18 (same as round 5 alone) — not because splitting isn't working (it is: confirmed live, e.g. a "Backsplash (Eldhús)" group that didn't fit anywhere whole got split into a placed single-front piece + a smaller remainder that then needed its own sheet), but because the *specific* Sökkulcover groups the user pointed at were already landing on well-utilized sheets thanks to round 5's fix, and the genuinely under-filled sheet (18 of 18, only 56% full) turned out to have nowhere better to send its 7 items — checked every existing column across sheets 1–17 for width **and** leftover length against those 7 items' actual dimensions, found zero matches. That's a structural ceiling of the current "fixed-width shelf/column" packer, not a splitting-logic gap: a column's width is locked to whichever group claims it first, so leftover space fragments into slivers (tens to a couple hundred mm of leftover length per column) too small for any real front, even though the *sum* of that leftover across 17 sheets is substantial. Getting past this would need a genuinely different packing model (free-rectangle/maxrects, not shelf/column) — a bigger, separate piece of work, flagged to the user rather than attempted here. The splitting mechanism itself ships regardless since it's a strict improvement-or-no-op (only ever activates when the whole-group placement already failed) and will help on projects/groups where the old round-5-only packer really was leaving reachable slack on the table.

**Office round 5 — front-row overlap fix, collapse-to-scan, better auto-nest (2026-09-14)**, a bug-fix + one "more functional" ask, ahead of moving into the WinCut/F2 cutting-sequence work: (1) **Fixed real text overlap in the front-row** — `.grp-panel button.small`'s round-4 size bump (7px/11px) pushed `.front-row`'s 4 action buttons (⚠⎘↑↓) plus grip/idx/dim wide enough that `.fn` (the front label, `flex:1;min-width:0`) got squeezed to a **computed 0px width** at the sidebar's actual size (`.side-col{flex:0 1 320px}` — fixed regardless of window width, confirmed on a 1440px viewport too), and its text kept painting past its own zero-width box, overlapping the dimension/buttons next to it. Root-caused with a live DOM check on a real 1-front group ("Úthlið/blindlok") — bounding rects showed `.fn` at exactly `left===right`. Two-part fix: (a) the round-4 button enlargement is now scoped to `.grp-actions button.small` only (the roomy group-level action row), not the whole `.grp-panel` — front-row buttons are back to their original compact size; (b) `.front-row` is now `flex-wrap:wrap` and `.fn` has `flex:1 1 90px;min-width:90px` (was `flex:1;min-width:0`) plus `overflow:hidden;text-overflow:ellipsis;white-space:nowrap` — so a front-row that still doesn't fit on one line wraps the buttons to a second line instead of squeezing `.fn` to nothing, and `.fn` itself can never go below a readable 90px or silently overlap again even in a worse future squeeze. Verified via pairwise bounding-rect overlap detection across all front-row children (zero overlaps) and a 6-front group (every `.fn` > 0px). (2) **Front lists collapse until the group is selected** — `.front-list` (every individual front row) is now only rendered when `sel` (was unconditional, so a 6+-front group like "Sk 19,21" always showed all 6 rows in full, even collapsed/unselected — a lot of scroll for scanning). Collapsed groups now show just the existing one-line `.gm` summary ("N frontar · WxH mm · plata N"); clicking the group (already the existing `pickGroup` interaction) expands it. Added a ▸/▾ chevron in the `<h4>` so the expand affordance is visible. **Trade-off, disclosed not fixed**: the panel's own HTML5 drag-and-drop for moving a front *between two different groups* (`wireFrontRowDnD`) needs both groups' `.front-row` elements in the DOM, which now only exist for the one selected group — so panel-drag between groups no longer works unless you happen to select the destination (impossible, `selectedGid` is single-valued). The primary, documented way to move a front between runs — dragging it on the board SVG itself (`enableFrontDrag`) — is completely unaffected; only the secondary panel-list drag path lost this specific case, and reordering *within* a group (the common panel-drag use) still works fully once that group is selected/expanded. (3) **`autoNest()`'s processing order changed from length-descending to width-descending** (`groupWidth(b)-groupWidth(a) || groupLen(b)-groupLen(a)`) — the "raða sjálfvirkt" packer stacks groups into fixed-width columns (`col.w` locked in by whichever group first claims that column), so a *wide* group processed *late* often couldn't fit any existing column (nothing already that wide) and got forced onto its own new, mostly-empty sheet — exactly the "lots of free space but still on separate sheets" the user flagged on T-33 (a 2050mm-wide-but-368mm-short "Sökkulcover Fatask" group, alone on sheet 21 with 2400mm+ of unused length below it, because being *short* meant it was processed dead last in the old length-first sort, by which point nothing narrower was left to backfill under it). Verified against T-33's real 84-group data by simulating both sort orders (and a couple of alternatives — area-descending, and a best-fit column/sheet search instead of first-fit) client-side before touching the real function: width-descending is the clear winner, **22→18 sheets, 69.8%→81.4% yield**, no benefit from adding best-fit complexity on top of it. Ran the real `autoNest()` on T-33 afterward to confirm the live function matches the simulation (18 boards, zero `detectOverlaps()`, zero `unplacedGroups()`) — the previously-lone "Sökkulcover Fatask" now shares a sheet with 5 other groups, interleaved with "Úthlið"/FRE groups exactly as the user said was fine ("feel free to separate Sökkulcover and ÚH/úthliðar freely" — there was never an explicit keep-together rule for those categories at the board level to begin with; they just happened to cluster as a side effect of the old length-only sort, which width-first sorting breaks up naturally). No change to within-group nesting, orientation logic, or the guillotine strip packer for a single run's own composite panel — only which *sheet/column* a whole group lands in.

**Office round 4 — stable run colours, decluttered stats, sidebar polish (2026-09-14)**, a 6-item batch, "look at the arrangement of fronts and a little more formatting" before touching the actual WinCut cutting logic: (1) **Run colour is now keyed off the group's own stable id, not its position in `groups`** — `groupColor(g)` hashes `g.id` into the `COLORS` palette instead of `COLORS[groups.indexOf(g) % COLORS.length]`. The old index-based scheme meant splitting/merging/adding *any* group shifted every later group's array index, silently recolouring runs on completely unrelated, untouched sheets — confusing and exactly what was reported. Replaced at all 4 call sites (`renderBoards`, `enableDrag`'s ghost, `renderPanel`, `guideSvg`); verified by splitting a group (array grows 40→44) and confirming zero of the other 39 groups' fill colours changed. `gi` (the group's `groups.indexOf`) is still used separately for the `#N` numbering shown in the board header band and panel `<h4>` — unrelated to colour, left alone. (2) **Noise reduced**: `#statRow` now shows only `N plötur (WxL)` — dropped `æðahópar · N frontar`, `m² frontar`, `nýting %`, and `samfelld æð` entirely (`renderStats()` gutted to match); the per-board head's `m² notað · % afgangur` span is gone too, and the done-pill lost its `m²` figure — `boardUsage()` (which only fed those two removed displays) was deleted outright rather than kept unused. (3) **The "Drög" status bar is brighter** — `.plan-bar.draft`'s border/background lightened (`#6b5a22`/`#201c12` → `#8f7530`/`#332912`) and the word `Drög` itself is now set in the existing gold accent (`#f5c518`, same tone as `.badge.grain`) instead of plain white, so the eye actually catches "this is a draft" instead of it reading as just another dark bar. (4) **Sidebar "→ plata N" button-per-sheet list replaced with a single `<select>` dropdown** (`onchange="sendToBoard(...)"`, same pattern as the existing "sameina við…" merge select) — scales to any number of sheets without the row of buttons wrapping across several lines. (5) **Clicking a piece now scrolls the sidebar to it** — `selectGroup(gid)` (fired by both the ⠿ run-drag handle and a front-cell click/drag, on `pointerdown`) calls a new `scrollPanelToGroup(gid)` after `renderPanel()`; `pickGroup(gid)` (the panel's own click-to-select) calls it too. Needed `data-gid` added to the `.grp` panel row so it's reliably queryable. Used **instant** scrolling (`scrollIntoView({block:"nearest"})`), not `behavior:"smooth"` — smooth scroll relies on `requestAnimationFrame`, which browsers throttle/pause for a backgrounded tab, and testing against a non-fronted preview tab showed `window.scrollY` never moving at all with `smooth`; instant scroll is also more honest to "jumps to that piece" than an animated one. (6) **Sidebar buttons enlarged a bit** — `.grp-panel button.small, .grp-panel select.small { padding: 7px 11px; font-size: 0.85rem; }` (was 5px/9px, 0.8rem), scoped to the side panel only so the toolbar/board-head buttons are untouched. **Caught and fixed during verification**: removing the per-group `gi` computation at the colour call sites broke the board SVG header band's `#N` label (`gi is not defined`, blanked the whole plan) — `gi` needed to stay for numbering even though colour no longer uses it; fixed by keeping both. **Found, not fixed** (pre-existing, unrelated to this round): T-15's saved draft has a real group-overlap — `detectOverlaps()` returns 5 pairs across boards 1 and 10 (Sk 29 ↔ SK 14, Sk 1 ↔ UHUEF LED, etc.), and `settleAll()` doesn't clear it, so whatever caused it isn't self-healing. Harmless for now since `confirmPlan()` hard-blocks on overlaps, but worth the user's attention before that plan is ever confirmed — flagged, not touched, since resolving it means understanding why `settleBoard` didn't already prevent it, which is packer-logic work outside this "reduce noise" round's scope.

**Medium UX batch (2026-09-09)**: **confirmed-plan drift** — `curPlan.confirmedJSON` holds the JSON the floor currently has; once a confirmed plan is edited (`schedulePlanSave` un-ticks it) the plan-bar turns amber "⚠ Óstaðfestar breytingar — gólfið er enn með fyrri staðfestu útgáfu" and the confirm button reads "✅ Staðfesta breytingar". **Save-failure** — `savePlan`'s catch calls `showSaveError(wasConfirm)` → a sticky red `#saveErrBanner` with a "Reyna aftur" (`retrySave`) button; `planSaveFailed` blocks `confirmPlan()` until a save succeeds (`clearSaveError`). **Settings popover** — board W/L, kerf, trim, grid moved into a `#settingsPop` dropdown off a "⚙ …" button that shows a live summary (`syncSettingsBtn`); office-only, closes on outside-click. **Actionable overflow** — `renderOverflow` splits too-long (offers a one-click `setTrim(x)` "Minnka snyrtingu í X mm" when reducing trim would fix it) from too-wide (can't fix — split or check Airtable). **Per-board guide progress** — `renderGuide` shows "Plata 3/10 · skref 3/5 · 2 ✓", board-boundary ticks on the progress bar, and the current board's nav button shows its own "(3/5)". **Sheet legibility** — each run gets a 1px dark halo (butt-joined similar-colour runs read apart), a `#N` number prefix in the header + side-panel `<h4>` (stable cross-reference), and v/s runs get continuous grain streaks spanning the whole run (was per-cell, broke visually at every kerf).

**Undo/redo** (⌘Z / ⌘⇧Z / ⌘Y, plus ↶ ↷ toolbar buttons): `history` is an array of `serializePlan()` strings; `pushHistory` is called from `saveLayout` (so every committed mutation snapshots), guarded by `applyingHistory`/`loadingPlan`. `undo`/`redo` walk `histIdx` and `applyPlan` the stored state. `resetHistory(); pushHistory()` sets the baseline at the end of `selectMaterial` / `openFloorPlan`. Keydown handler ignores it while an input is focused or the guide overlay is open.

**Sticky side panel**: `.side-col` is `position:sticky; top:14px; max-height:calc(100vh-28px); overflow-y:auto` so the Æðahópar list scrolls on its own while the boards scroll the page.

The sawyer drags runs, reorders/​splits/​merges, sets kerf/trim, then either **prints** (each board = one page, scaled diagram + rip/crosscut sequence) or runs **"▶ Saga með leiðsögn"** — a full-screen one-step-at-a-time guide (`buildCutPlan()` → `#guideOverlay`): per board → trim → per vertical strip: rip strip, crosscut each front off the leading edge top→bottom, with a narrower run getting a follow-up rip-to-width step; horizontal runs = crosscut block then rip into N. Each step shows the one fence/stop number big, a board mini-diagram with the cut line + piece highlighted, and the finished piece(s) to label immediately. Arrow keys / space to advance, Esc to close, progress saved in `localStorage` (`grain:guide:*`) so an interrupted sawyer resumes. Read-only (no Airtable write-back yet). Manual layout is persisted per project+material in `localStorage` (`grain:layout:*`); settings in `grain:settings`. `Efni:` (resolved decor incl. customer material) was added to Sögunarlisti's `ALLOWED_FIELDS` for this page. `normalizeGroups().fits` allows **22 mm of length slack** before splitting a grain stack (`groupLen(g) <= maxL + 22`) — better a tiny overflow past the trim line than cutting a grain-matched run in two (the far sheet edge isn't trimmed anyway); a genuine overhang still shows in the "Passar ekki á plötu" warning. The `Vígindi (timbur mynstur) 🌳` / `Stemmir 🪚` checkboxes on Sögunarlisti exist but are unused in practice, so grouping is inferred, not flagged.
- **grain.html query-string encoding**: its `airtableFetch` builds the query string with `encodeURIComponent`, NOT `URLSearchParams` — `URLSearchParams` emits `+` for spaces and `vercel dev`'s request parser does not decode `+` back to space, so `{Field Name}` refs in `filterByFormula` fail with a 422 locally (works in prod, breaks in local dev). cutlist/fraesing use `URLSearchParams` and only work because prod decodes `+`; follow grain.html's approach for any new page.
- On USB export, cutlist.html writes two fields per piece back to Sögunarlisti: `Skurðarskrá` (the `CUTLST00-N-<material>` filename it landed in) and `Skurðarnúmer` (its 1-based position in that file's piece list = the "Code" Cutty shows on the left of the piece table — one code per line, quantity is separate; Cutty keeps file order). labels.html shows `Skurðarnúmer` as the "Kóði" column so the saw operator can match a piece coming off the saw to its label row. Codes restart at 1 per file, so labels.html prefixes each run with its file number (`2·5–7`) unless the Skrá filter is already narrowed to one file. Only populated for projects run through Cutty after 2026-08-31; older projects show "–". Both fields are in `WRITABLE_FIELDS`/`ALLOWED_FIELDS` for Sögunarlisti in api/airtable.js.
- Local dev: `.claude/launch.json` has a `vercel-dev` config (port 4322) that sources `.env.local` before running `vercel dev`, since `vercel dev` does **not** load `.env.local` into the function process on its own here — confirmed by direct testing (a custom test var placed in `.env.local` never showed up in `process.env` until the launch command explicitly `source`d the file first). `AIRTABLE_TOKEN`/`WEBHOOK_SECRET` are marked Sensitive in Vercel, so `vercel env pull` returns them empty — get a real PAT from the user and write it into `.env.local` (gitignored via `.env*`) to test locally.
- When building a query string for `/api/airtable`, always percent-encode with `encodeURIComponent` (not `URLSearchParams`, which turns spaces into `+`). The incoming request parser here does not decode `+` back to space, so a `+` in a `filterByFormula` field reference (e.g. `{Field Name}`) survives as a literal plus and Airtable's formula parser rejects it as an unknown field. `api/airtable.js` also normalizes any stray `+` back to `%20` before forwarding to Airtable, as a second layer.
- Stimpilklukka (`/klukka`) reads/writes `Starfsmenn`, `Stimplanir`, `Verktímar`, `Fjarvistir`, `Orlofsbeiðnir` and reads `Tækifæri` (project list for the clock-out split) in the same Airtable base. Each `Starfsmenn` record needs a `PIN 🔢` value set for that employee to be able to clock in/out — there's no UI for assigning PINs, set them directly in Airtable.
- `Stimplanir` is one row per **shift**, not per tap: `INN` creates a row with `Inn` set and `Út` blank; `ÚT` finds that employee's open row (`Út` blank) and PATCHes `Út` onto it. "Currently clocked in" = an open row exists. Matching an employee's open shift is done by exact name (`{Starfsmaður}="..."`), not record ID — referencing a linked-record field in an Airtable formula resolves to the linked row's *display name*, not its ID, so `SEARCH(recordId, ARRAYJOIN(...))` silently never matches (found this the hard way — always test formula fields against real data, not just isValid).
- Dagvinna/yfirvinna split is computed entirely in Airtable formula fields on `Stimplanir` (`Dagv. start/endir`, `Hádegi start/endir`, `Vinnugluggi start/endir`, `Vinnugluggi (mín)`, `Háðegi skörun (mín)`, `Er frídagur`, `Er hálfur frídagur`, `Dagvinna (klst)`, `Yfirvinna (klst)`, `Samtals (klst)`) — the app never does this math client-side, and the daily HR automation only *reads* the resulting `Dagvinna (klst)` / `Yfirvinna (klst)`. **Schedule (per [Byggiðn](https://byggidn.is/vinnutimi/), overtime counted from when day work ends until it begins next morning):** `Framleiðsla` = **08:15**–16:30 Mon–Thu / 08:15–13:15 Fri, unpaid 12:00–12:45 lunch Mon–Thu. `Starfsheiti 💼 = Sölumaður / Arkitekt` = **09:00–17:00 Mon–Fri**, unpaid 12:00–12:30 lunch every weekday (30 unpaid + 30 paid rest). Overridable per employee via `Starfsmenn.Vinna byrjar` / `Vinna endar` / `Vinna endar (föstudag)` (text `HH:MM`, blank = role default); the formulas branch on the `Starfsheiti` lookup + those fields (with `&""` coercion). Anything after the window, Sat/Sun, or a rauður dagur = yfirvinna. Time before the day-start is excluded from *both* (business decision) — Yfirvinna is computed from `MAX(Inn, Dagv. endir)` onward, not `Samtals − Dagvinna`. **No** Yfirvinna 1/2 rate-tier split or night rates — only the hour classification. See **## HR / Launakerfi** below for the whole picture.
  - **Lunch must be subtracted from the clipped dagvinna window, not the raw shift.** Originally `Háðegi skörun (mín)` overlapped the lunch window against raw `Inn`/`Út`, which happened to work when the dagvinna window always contained the lunch window — but broke (went *negative*) once holidays could end the dagvinna window at/before noon. Fixed by adding `Vinnugluggi start`/`Vinnugluggi endir` (the shift clipped to the dagvinna window) and computing the lunch overlap against *those* instead. If you ever touch these formulas again, re-run the regression cases from the session that built this (~10 synthetic shifts incl. a half-holiday case) before trusting a change.
  - **Public holidays** — `Er frídagur` / `Er hálfur frídagur` now just read the `Frídagur` / `Hálfur frídagur` checkboxes on `Stimplanir` (no more hardcoded date lists). Those checkboxes are set at clock-in by the `Stimplun → frídagur + tímabil` automation, which matches the `Inn` date against the **`Frídagar 📅`** table (`tblXoWJacAvfrXgty` — `Dagsetning`, `Hálfur dagur`; 2026–2028 seeded from fridagar.is). Yearly upkeep = add next year's rows to `Frídagar 📅`. Aðfangadagur / Gamlársdagur are `Hálfur dagur` (dagvinna until 12:00, afternoon yfirvinna).
- **Fjarvistir** (`tbl3e5o0Klv9RcNQ4`) is the sick-day log: one row per employee per sick day (`Dagsetning`, `Starfsmaður`, `Tegund`=`Veikindi`). The kiosk's "Tilkynna veikindi í dag" button marks *today* only (no back-dating, no multi-day range — tap again each day if out longer) and closes any currently-open shift, since sick leave doesn't require clocking in/out first. It works from any device, not just the shop kiosk — same PIN-gated `/klukka` URL. **Airtable gotcha:** a `date`-type field compared directly with `{Field}="2026-07-11"` in a formula silently matches nothing; wrap it in `DATETIME_FORMAT({Field},'YYYY-MM-DD')="..."` instead (`dateTime` fields don't have this problem, only bare `date` fields do — Fjarvistir's `Dagsetning` is a `date` field, Stimplanir's `Inn`/`Út` are `dateTime`).
- **Sick → vacation rollup** — Björninn's policy is 2 paid sick days/month per active employee; whatever's unused rolls into `Auka orlofsdagar 🌴` (bonus vacation days) on `Starfsmenn`, 1:1, **capped at a running balance of 10**. This is now handled entirely in Airtable by a "Run a script" step in the **`MD 🗓️ - DAGLEGT ⏰`** automation (see the full HR system — Launatímabil, holiday/sick pay, orlof engine — which lives there and in the `Launatímabil 🧾` table). The old Vercel cron `api/monthly-sick-rollup.js` was removed 2026-08 once the Airtable version took over. Paid sick days now also pay a standard day's hours into `Launatímabil`, and paid public holidays not worked do the same (`Frídagar 📅` table).
- **Grammatical gender**: `Starfsmenn.Kyn` (singleSelect, `KK`/`KVK`) drives which Icelandic verb/adjective forms stimpilklukka uses (mættur/mætt, skráður/skráð veikur/veik, stimplaður/stimpluð). Defaults to `KK` in the app (`isKK = record.fields["Kyn"] !== "KVK"`) if the field is blank for an employee — fill it in per employee directly in Airtable, no kiosk UI for it.
- **"Mínar stundir"** (self-service hours) sums the current **calendar month's** *closed* Stimplanir shifts (`{Út}!=BLANK()`) client-side — an info view only, no longer part of any confirmation flow. Note it's still calendar-month while payroll runs on 29→28 periods (`Tímabil`); update `stimpilklukka.html` to filter by `Tímabil` if the mismatch ever matters. Open shifts are excluded (shown as a note).
- **Orlofsbeiðnir** (`tbljdg6uxEfHE7uCU`) — `Staða` (Í bið/Samþykkt/Hafnað) is forced to "Í bið" server-side in `api/airtable.js`'s `FORCED_CREATE_FIELDS` — the client never sends it, so a tampered request can't self-approve. Manager reviews/flips `Staða` directly in Airtable; no approval UI in the kiosk. Works from any device, same as sick-marking. There **is** now a real orlof balance (`Starfsmenn.Orlofsdagar eftir`, shown on the request as `Orlofsdagar eftir (staða)`), and flipping `Staða` to `Samþykkt` makes the daily automation create the matching `Fjarvistir` (Orlof) rows — see **## HR / Launakerfi**.
- **`/eigandi` — owner phone clock-in** ([api/eigandi-clock.js](api/eigandi-clock.js)): the kiosk device lock ties INN/ÚT to being at the shop; owners don't want that. `/eigandi` is `stimpilklukka.html` with `OWNER_MODE` (`location.pathname === "/eigandi"`). The PIN goes to `POST /api/eigandi-clock` which verifies it belongs to an `Eigandi 👨🏻‍💼` and returns `{ token, record, shortcut }`. `token` is `ownerDayToken()` — HMAC of the date keyed with `KIOSK_DEVICE_SECRET`, rotates daily, never exposes the secret; `isKioskPaired()` in `api/airtable.js` accepts today's or yesterday's alongside the real secret, so the page's own Stimplanir/Verktímar writes go through unchanged. `/klukka` and the shop tablet are untouched — no re-pairing.
  - **GPS / Apple Shortcuts**: `POST /api/eigandi-clock?k=<recordId>~<hmac>&action=inn|ut|toggle` clocks in/out server-side with no PIN — for a "When I Arrive" personal automation. POST-only (a link-preview GET can't clock anyone in). `k` is `shortcutToken()`, stable per owner, shown as a copyable URL by the "⚙️ Sjálfvirk stimplun" button after login. No per-owner revoke — rotating `KIOSK_DEVICE_SECRET` kills every token (kiosk + shortcuts).
  - **Catch-up project split**: an auto clock-out (`action:"ut"`) just closes the shift — a Shortcut can't do the interactive Verktímar split. So on `/eigandi` login, `checkCatchUp()` looks for the owner's own closed shifts (last 5 days, ≥30 min, not already covered by a `Verktímar` row, not skipped this session) and opens the split screen for the oldest one first, then returns to the employee screen. `/klukka` (non-owner) never runs this.
- **Kiosk device lock**: INN/ÚT (Stimplanir POST/PATCH) require an `X-Kiosk-Token` header matching the `KIOSK_DEVICE_SECRET` env var — `KIOSK_LOCKED_TABLES`/`isKioskPaired()` in `api/airtable.js`. Sick-marking and vacation requests are untouched, still work from any device. Pairing: visiting `/klukka?setup=<KIOSK_DEVICE_SECRET>` once stores the secret in that browser's `localStorage` and strips it from the URL via `history.replaceState`; re-pairing (revisit the URL) is needed if the tablet's browser data is ever cleared or the tablet is replaced. Closing a shift from `markSick()` (someone was clocked in, then marked sick remotely) is best-effort — it's still a Stimplanir PATCH, so it silently no-ops on an unpaired device rather than blocking the sick-marking itself.
- **Interfaces are built manually in Airtable, not by me**: I have no API access to add elements to an *existing* interface page (only create whole new pages, which show up as separate sidebar tabs, not sections stacked on an existing page) and no way to edit/rename singleSelect choices after creation (only whole-field create). Anything involving those has to be described step-by-step for the user to do themselves in the Airtable UI.
- **`Mánuður 🗓️`/`Ár 🗓️` on Stimplanir are real single-select fields, not formulas** — deliberately, so they work as clean pick-a-value dropdown filters on Interface pages. (A formula/date field only offers Airtable's date-*comparison* filter — is/is before/is after — not a value picker; only checkbox/date/dateTime/select/collaborator/linked-record field types get the dropdown-of-values treatment.) The kiosk computes and sends both directly in `punchAction()`'s POST; `api/airtable.js` sends `typecast: true` so a non-matching choice self-adds rather than hard-failing. `Ár 🗓️` covers 2025–2030. **Payroll filtering now uses `Tímabil 🗓️` instead** (single-select, same reasoning) — set by the `Stimplun → frídagur + tímabil` automation, mirrors the `Tímabil` formula. Choices run to 2028-12 on both `Stimplanir` and `Launatímabil`.
- **Per-project time — `Verktímar 🕒`** (`tblGu27c2fcwN2i2n`): on **ÚT**, `stimpilklukka.html` first PATCHes `Út` onto the shift (so the clock-out is safe even if the next step is abandoned), then shows an allocation screen where the employee splits that shift's hours across the projects they worked on. One `Verktímar` row per slice: `Dagsetning`, `Klst` (worker-entered, 0.5h steps), `Starfsmaður` link, `Verkefni 📣` link (blank for the buckets), `Verkflokkur` (`Verkefni`/`Sölur`/`Annað`), `Stimplun ⏱️` link back to the shift row. Rolls up to `Tækifæri › Vinnutími starfsmanna (klst) 🕒` (SUM) — compare with the estimate `samtals framleiðslutími Rollup ➖📦 (Line item's) klst` — and `Starfsmenn › Verkefnatímar samtals (klst) 🕒`.
  - **Stimplanir is untouched** — still one row per shift, all payroll formulas unchanged. The split lives entirely in the child table. This was a deliberate choice over segmenting Stimplanir into per-project rows (which would have put the dagvinna/yfirvinna/lunch math at risk).
  - **`Klst` is the raw shift duration split**, not paid hours. `Samtals (klst)` is itself just `(Út − Inn)` (no lunch removed), so "divide your Samtals across projects" = divide raw hours. Target is `(Út − Inn)` rounded to the nearest 0.5; the odd minutes ride silently on the project the worker put last. Lunch/pre-08:00 nuance only matters for the dagvinna/yfirvinna split, which stays in Stimplanir.
  - **Which projects show is driven by `Starfsheiti 💼`** (now in Starfsmenn's `ALLOWED_FIELDS`): `Framleiðsla` → active workshop jobs only (won + `Klárt til framleiðslu` + not `Má Afhenda`/`Afhent`); `Sölumaður / Arkitekt` → a `Sölur` bucket + won jobs still in `For-skipulag`/`Í skipulagningu`/`Yfirfara` **plus any `🔥 Líklegt tækifæri` deal** (pre-win design work); every other role → both lists + `Sölur`. Everyone also gets an `Annað` bucket so the split can always reach 100%.
  - **Mandatory**: submit is blocked until the remainder is exactly 0. Escapes that leave the shift closed but the split unwritten (manager fills in on `Verktímar`): a "Sleppa í bili" link shown only if the project list fails to load, and a 120s inactivity timer that frees the kiosk.
  - **Kiosk-device-locked** like Stimplanir — `Verktímar` is in `KIOSK_LOCKED_TABLES`, so the split POSTs need the `X-Kiosk-Token` header (verified: without it → 403 `DEVICE_NOT_PAIRED`).
  - **Rollup precision**: Airtable rollups default to 0 decimals and the API can't set precision — bump `Vinnutími starfsmanna (klst) 🕒` (and the Starfsmenn one) to 1 decimal in the field's formatting UI if the rounding bothers anyone. The underlying value is exact.
  - Replaced the old `/stod` (Vinnustöðvar) page + `Stöðvatímar ⏱️` table, both deleted 2026-08-27 — that kiosk (anonymous Kantlíming/Spónlagning/Lökkun station timing) was never really used (one test row) and this covers the same "how long per project" goal.

## HR / Launakerfi (Airtable — no Vercel)

Built 2026-08. Full chain: clock (`/klukka`) → pay period → dagvinna/yfirvinna split → holiday/sick/orlof pay → frozen monthly close → CSV for the bókari. **No employee sign-off step** — staff just get a payslip (ask for a timesheet → export from `Stimplanir`). Everything lives in Airtable: tables + formulas + one big "Run a script" step in the `MD 🗓️ - DAGLEGT ⏰` automation + one shift-created automation.

### Pay periods — 29th → 28th
- A launatímabil runs **29th of one month to 28th of the next**, labelled by the month it *ends* in (`2026-08` = 29 Jul – 28 Aug), paid the **1st** of the next month.
- Clock-in on days 1–28 → that month's period; days 29–31 → next month's.
- `Stimplanir.Tímabil` (formula) computes it; `Stimplanir.Tímabil 🗓️` (single-select, choices → 2028-12) is the same value for dropdown filtering, set by the `Stimplun → frídagur + tímabil` automation. `Launatímabil` has `Tímabil frá` / `Tímabil til` (dates) + `Tímabil 🗓️`.
- Non-leap February: that period starts 1 Mar (no 29 Feb) → a 28-day period. No days fall through the cracks.

### `Launatímabil 🧾` (`tblSQ0FfGQeBttaH2`) — one frozen row per employee per period
- Shifts + `Fjarvistir` linked in by the automation; hours arrive as rollups (`Dagvinna klst`, `Yfirvinna klst`, `Samtals klst (hrátt)` = raw Út−Inn incl. lunch — *not* payroll basis).
- `Frídagar greitt (klst)` + `Greiðir frídagar` (text list) — **paid public holidays not worked**: 7,5 Mon–Thu / 5,0 Fri / 3,75 half (Framleiðsla); 7,5 any day / 3,0 half (Sölu). Only if employed that date and no shift.
- `Veikindi greitt (klst)` — **first 2 sick days/period** (chronological, half-day = 0,5) paid at that weekday's standard hours; day 3+ unpaid.
- `Dagvinna alls (klst)` = Dagvinna rollup + Frídagar greitt + Veikindi greitt ← **the dagvinna number on the payslip / CSV.**
- `Greiddar klst` = Dagvinna alls + Yfirvinna.
- Króna: `Dagvinnulaun` (Dagvinna alls × `Kaup per klst`), `Yfirvinnulaun` (× `Yfirvinnagjald` = Kaup × 1,56), `Orlofsinneign (kr)` (`Orlofs%` × laun — paid out monthly), `Heildarlaun f. skatt`.
- Workflow: tick `Læst` → the automation copies Dagvinna alls / Yfirvinna / Greiddar / Heildarlaun into the `(læst)` fields (frozen — a paid period never changes). `Orlof-rúllun búin` guards the sick→orlof rollover from double-counting. `Staðfest af starfsmanni` fields exist but are unused.
- The bókari CSV = a grid view filtered `Læst` ✓ + `Tímabil 🗓️`, columns `Nafn · Kennitala · Dagvinna alls (klst) · Yfirvinna klst`, Download CSV. (`Kennitala` source field is a *number* → drops leading zero for anyone born on the 1st–9th.)

### Orlof — real balance on `Starfsmenn`
- Samiðn/Byggiðn seniority ladder: `Menntun` (Ekkert/Í námi/Sveinspróf/Meistarabréf) + `Ár í iðngrein við ráðningu` + `Starfsaldur hjá Björninn (ár)` → `Orlofsþrep` → `Orlofsréttur (dagar)` (24/25/27/28/30) + `Orlofs%` (10,17…13,04). Skilled (sveins/meistara) get the skilled table.
- `Orlofsdagar teknir (þ. orlofsár)` = rollup of `Fjarvistir` (Tegund=Orlof) falling in the current orlofsár (1 May – 30 Apr; `Er í yfirstandandi orlofsári` helper). `Orlofsdagar eftir` = réttur + `Orlof frá fyrra ári` + `Auka orlofsdagar 🌴` − teknir.
- **Model**: orlof % is paid out monthly (`Launatímabil.Orlofsinneign`); orlof days are planning/tracking only, they don't pay extra (the money was already paid).
- `Auka orlofsdagar 🌴` = bonus days from unused sick leave (2/month, uncapped days used doesn't go negative, **running balance capped at 10**).

### `Fjarvistir 🤒` (`tbl3e5o0Klv9RcNQ4`)
One row per employee per day. `Tegund`: Veikindi / Veikindi barna / Vinnuslys / Orlof / Launalaust leyfi / Námsleyfi / Annað. + `Hálfur dagur`, `Greitt?`, `Vottorð`, `Athugasemd`.

### Automations
1. **`MD 🗓️ - DAGLEGT ⏰`** (cron 07:00) — its 2nd "Run a script" step is the whole HR engine:
   - **Current period**: build/update `Launatímabil` daily (link shifts + fjarvistir, compute holiday/sick pay).
   - **Uppgjör**: finalize last period *once* (self-heals via `Orlof-rúllun búin`) — sick→orlof (`+MAX(0, 2 − veik)` into `Auka orlofsdagar`, cap 10), then Chat "tilbúið að læsa".
   - **Frysta**: `Læst` ticked → copy into `(læst)` fields.
   - **Reminders → Google Chat** (space `AAQAmeYUzJc`, webhook hardcoded): forgot-to-clock-out, 3+ consecutive sick days, pending `Orlofsbeiðnir`, approved leave → creates `Fjarvistir` (Orlof) rows, reynslutími/starfsafmæli within 7 days, day 28 (period ends → clock out), day 1 (review & lock), 15 Mar (unused orlof), 1 May / 1 Jun / 1 Dec (orlofsár / uppbætur).
   - `PAYROLL_TYPES = ["Fast ráðinn 👷","Verktaki 👨‍🎨","Lausráðinn "]` — only these get a launatímabil (owners/robot skipped).
   - **The MCP API can't create/edit `customScript` nodes** (`readOnlyNodeType`) — all script changes are handed to the user as paste-in-full or find/replace for the Airtable UI.
2. **`Stimplun → frídagur + tímabil`** (record created on `Stimplanir`) — matches `Inn` date to `Frídagar 📅` → sets `Frídagur` / `Hálfur frídagur`; computes `Tímabil 🗓️`.

### Deal payment split (`Tækifæri`, replaced 2 automations)
`Skipting greiðslu` (single-select: `100%` / `50% - 50%` / `40% - 30% - 30%`) + `Greiðsla 2 greidd` / `Greiðsla 3 greidd` checkboxes + `Greiðsla 1 (staðfesting)` (formula, ✅ once `Staða í söluferli = 🏆 Tækifæri unnið`) → **`Greitt (kr) 💰`** formula (`💰 Tilboðsupphæð` × ticked installment %s) + `Eftirstöðvar (kr) 💰`. Old `Það sem kúnni hefur borgað 💲` / `Eftirstöðvar greiddar` kept but unused. Interface hides Greiðsla 2/3 based on the split.

### Yearly / periodic upkeep
- Add next year's rows to `Frídagar 📅`.
- Extend `Tímabil 🗓️` choices (both `Stimplanir` and `Launatímabil`) before 2029.
- Byggiðn kjarasamningur changes → re-check the orlof ladder days/% (`Orlofsréttur` / `Orlofs%` formulas) and the 62.000 kr orlofsuppbót figure in the daily script.
- Confirm with the bókari periodically: pre-day-start time stays unpaid; orlof paid-out-monthly model; the flat dagvinna/yfirvinna split (no YV1/YV2).
